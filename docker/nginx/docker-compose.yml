version: "3"

services:
  nginx:
    image: "nginx:1.23.0"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 8080:80
    environment:
      - NGINX_HOST=127.0.0.1
      - NGINX_PORT=80
    # network_mode: host
