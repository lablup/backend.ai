# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.11"
  jobs:
    post_build:
      - cp docs/manager/rest-reference/index.html _readthedocs/html/manager/rest-reference/index.html
python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py

formats: all
