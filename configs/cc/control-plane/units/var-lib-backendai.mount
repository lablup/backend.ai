[Unit]
Description=Backend.AI attested state volume
DefaultDependencies=no
Conflicts=umount.target
Before=umount.target local-fs.target
After=systemd-remount-fs.service

[Mount]
What=/dev/mapper/backendai-state
Where=/var/lib/backendai
Type=ext4
Options=noatime

[Install]
WantedBy=local-fs.target
