[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "bai-storage-format"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "bai_storage_format"
features = ["pyo3/extension-module"]
