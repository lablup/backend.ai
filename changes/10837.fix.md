Fix delegated session creation (`owner_access_key`) to attribute sessions to the owner, not the requester. `UserScope` is now built from the owner's `user_uuid` and `user_role`, which also means scaling group access is resolved using the owner's permissions only (not the union of owner and requester).
