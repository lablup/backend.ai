Fix delegated session creation (`owner_access_key`) so that the `UserScope` carries the owner's `user_uuid` and `user_role` instead of the requester's. Previously, sessions created on behalf of another user were attributed to the requester (the admin) in the database row, leading to wrong container UID/GID lookups, broken customized image ownership checks, and incorrect session ownership in `sessions.user_uuid`.
