Add a VXLAN overlay cluster network (BEP-1079): the manager allocates each multi-node session a subnet and a VNI, and each agent builds its node's half of the overlay itself, encrypted with ESP, with an optional privileged helper so the agent needs no network capability. Selected with `network.inter-container.default-driver = "cni"`; the Docker Swarm driver is unchanged.
