Fix GQL `my_client_ip` returning the hive-gateway proxy IP by forwarding the `X-Forwarded-For` header from hive-gateway to manager subgraph requests (note: `allowed_client_ip` configurations that whitelisted the hive-gateway IP as a workaround should be reviewed, as the manager will now see the real client IP via GQL)
