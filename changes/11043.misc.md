Rename `UserPermission.user_uuid` to `owner_id` and add `main_access_key: str | None`. `KernelRow.to_kernel_info` populates the new field from the eagerly-loaded `user_row` relationship. No external behavior change.
