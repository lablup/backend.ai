Make the DOCKER install mode emit a conventional compose deployment: per-file `/etc/backend.ai` config mounts with image default commands, host networking only for the manager and agent, bridge networking with published ports for the other services, the halfstack merged into the single generated compose file (one file, one project), daemon-visible state under fixed system paths (`/var/lib/backend.ai`, `/tmp/backend.ai`, `/vfroot/local`) so no running service mounts the install directory, and the storage-proxy running as the installing user
