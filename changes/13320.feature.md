Add a Jinja-based `name_template` field to role presets so that roles instantiated from a preset are named per target scope (e.g. `{{ scope.name }}-member`) instead of reusing the preset's fixed name
