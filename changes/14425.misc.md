Remove the unused `common/dto/manager/v2/scaling_group` DTO package (`UpdateScalingGroupInput`, `ScalingGroupNode`, `PreemptionConfigInput` and their enums) and its unit tests; the `resource_group` DTO package has replaced it and no REST handler, adapter, GQL type, or CLI referenced it
