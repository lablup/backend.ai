Send the `X-RateLimit-Limit`, `X-RateLimit-Remaining` and `X-RateLimit-Window` headers on every manager response of a rate-limited request, including the HTTP 429 and error responses, instead of only on successful ones.
