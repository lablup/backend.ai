Add REST v2 CRUD endpoints for the `login_client_types` entity at `/v2/login-client-types/`, including an admin-only `/v2/login-client-types/search` endpoint with filtering, ordering, and pagination support.
