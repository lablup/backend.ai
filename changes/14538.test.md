Add scenario tests for every call of the resource slot adapter — the slot type catalog, the agent resource reads, the kernel allocation reads and the domain and project resource overviews — written one for one against the scenario table `resource_slot/KNOWLEDGE.md` now carries.
