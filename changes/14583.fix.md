Fix the kernel entrypoint failing on images without shadow-utils or coreutils (such as distroless images that ship only a shell) by editing the account files directly and hashing the session password in the kernel runner when the tools are absent.
