Implement event-based service discovery runtime integration with component registration events, Manager persistence handler, background stale-service sweep, and admin GraphQL API for the service catalog
