Authorize `owner_id` delegation in session enqueue so a caller can only create a session on behalf of another user when they hold session-create permission over that user's scope, closing a privilege-escalation hole where any authenticated user could impersonate arbitrary users (including admins).
