Add BEP-1050 proposing a Prometheus Query Preset System. This decouples PromQL template definitions from application code, allowing administrators to manage query presets at runtime via REST, GraphQL, and CLI.
