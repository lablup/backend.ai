Add ``groups.domain_id`` UUID foreign key referencing ``domains.id`` (NOT NULL, ``ON DELETE CASCADE`` / ``ON UPDATE CASCADE``), retain the legacy ``groups.domain_name`` string column without an FK constraint, and propagate the new identifier through ``GroupCreatorSpec``, the REST / GraphQL project create endpoints, and the model-store group creation path inside ``DomainRepository``.
