Deprecate the two order-field values that name no column — `ArtifactOrderField.SIZE`, whose size belongs to a revision rather than the artifact, and `ObjectStorageOrderField.CREATED_AT`, which the table has no column for. Both are ignored when ordering and are removed in the next release.
