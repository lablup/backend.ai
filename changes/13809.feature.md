Migrate the legacy `config/idle` etcd settings and the keypair resource policies' `max_session_lifetime`/`idle_timeout` into DB-backed idle checkers through the new `mgr idle-checker migrate-legacy` command, creating one checker per distinct legacy value and binding them to every project as disabled assignments
