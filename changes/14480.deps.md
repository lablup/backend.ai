Upgrade aiohttp, strawberry-graphql, orjson, PyJWT, click, msgpack, python-dotenv and memray past the versions reported as vulnerable; the click upgrade keeps `admin scaling-group`, `fair-share resource-group` and the two `mgr etcd` SFTP resource-group commands under their existing names.
