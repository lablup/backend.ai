Raise HTTP exceptions instead of returning them from the manager's API version middleware, the OpenID and keypair auth plugins, and the agent watcher's auth middleware; the manager's exception middleware now passes raised redirects through instead of rewriting them into a 400.
