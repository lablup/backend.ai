Raise HTTP exceptions from the manager's API version middleware and the agent watcher's auth middleware instead of returning the exception objects, so the manager's 400 for an unsupported API version is logged and counted like other client errors.
