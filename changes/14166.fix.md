Raise HTTP exceptions instead of returning them from the manager's API version middleware and auth plugins, the agent watcher's auth middleware, and the webserver and appproxy handlers; the manager and appproxy exception middlewares pass raised redirects through, and the webserver's security policy middleware applies its headers to raised exceptions as well. The converted 4xx and 5xx sites raise `BackendAIError` subclasses, so their responses carry an `error_code`.
