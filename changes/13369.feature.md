Accept `--output` on the client CLI subcommand itself (e.g. `backend.ai service list --output=json`) instead of only ahead of it at the root group; a subcommand-level value overrides the root-level one.
