Restructure the app-proxy SDK around a new `AppProxyClientRegistry` (`ai.backend.client.v2.approxy_registry`) so deployment-runtime domain clients are lazy-loaded with the same `create()` / `close()` / `@cached_property` lifecycle as `BackendAIClientRegistry`. `DeploymentChatClient` now takes a `BackendAIAppProxyClient` instance instead of a `ClientConfig`; obtain it via `registry.deployment_chat` (CLI) or by composing one explicitly (tests, embedding).
