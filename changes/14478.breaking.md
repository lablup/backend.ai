Answer the scope-entity relation from the virtual entity graph alone and drop the `association_scopes_entities` table, removing the listings that read the relation directly: the GraphQL `adminEntities` query, `Role.scopes` and the `EntityRef` types, the REST v1 and v2 entity search endpoints, and the `rbac.search_entities` SDK call with its `bai rbac entity search` command
