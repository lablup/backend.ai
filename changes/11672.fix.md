Fix `Route warming-up` never advancing to `RUNNING` when `health_check` is configured. The route health observer now scans `PROVISIONING+WARMING_UP` routes in addition to `RUNNING`, so initial health probes are written to Valkey while a replica is warming up.
