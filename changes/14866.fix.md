Create every vfolder group-writable, so that project folders and clones are writable from containers running as a per-user `container_uid` instead of only folders whose owner already carried the uid at creation time.
