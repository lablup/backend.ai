Fix the spurious `ModelServiceConfig.port` "field required" validation error when adding a model revision with a health check enabled but no explicit port, by making the model-definition draft merge override a field only when the higher-priority value is non-`None` (so an omitted `port` no longer clobbers the runtime-variant baseline port)
