Fix the spurious `ModelServiceConfig.port` "field required" validation error when adding a model revision with a health check enabled but no explicit port, by no longer materializing omitted GraphQL revision inputs as explicit `null` (which clobbered the runtime-variant baseline port during the merge)
