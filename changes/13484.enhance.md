Resolve a user's main keypair from the `keypairs` marker so that deleting an unrelated keypair no longer changes which policy their sessions and idle checks use, nor makes their reported `main_access_key` read as empty
