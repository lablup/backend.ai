Add replica-group scheduling-history commands to the SDK and CLI (`bai scheduling-history replica-group search / search-scoped`, the latter taking exactly one of `--deployment-id` / `--replica-group-id`).
