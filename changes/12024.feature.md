Add an on-demand `usage` field to the v2 `VFolder` GraphQL node, exposing the file count, used bytes, and quota limits of a virtual folder fetched live from the storage proxy only when the field is selected.
