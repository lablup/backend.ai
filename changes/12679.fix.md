Retain the system announcement message when disabling it: disabling now only hides the announcement instead of deleting the stored message, so re-enabling no longer requires retyping. Enabling still requires a non-empty message, and passing an explicit empty message clears the stored text.
