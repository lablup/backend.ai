Retain the system announcement message when disabling it. The message and its enabled flag are now stored under separate etcd keys, so disabling an announcement only hides it instead of deleting the message (re-enabling no longer requires retyping). Enabling still requires a non-empty message, and passing an explicit empty message clears the stored text.
