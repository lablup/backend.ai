Fix cursor pagination skipping rows that share the cursor row's sort value: `PaginationSpec` now builds the keyset condition on (sort column, tiebreaker) itself and reverses the tiebreaker for `last`/`before`, replacing the per-entity `by_cursor_forward`/`by_cursor_backward` conditions.
