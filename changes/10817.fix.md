Add per-client-type concurrent login session tracking. Clients now send their type (core, webui, fasttrack) during login, and concurrent session limits from keypair resource policy are enforced independently per client type.
