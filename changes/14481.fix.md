Apply `kernel-creation-concurrency` to the agent rather than to each create request: the limit is a statement about what a host can start at once, but a semaphore built per RPC call let N concurrent sessions each run the configured number, and the container creates that piled up could miss the manager's create deadline and take their session down.
