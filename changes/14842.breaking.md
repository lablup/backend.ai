Read runtime variants, their presets, prometheus query presets and their categories, login client types, resource slot types and deployment presets in the public scope: these reads now ask for read permission there instead of only for a logged-in caller, so an id or a name nothing answers to is refused rather than reported missing.
