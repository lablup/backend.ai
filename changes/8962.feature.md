Add `integrity_error_checks` to `CreatorSpec` and `BulkCreatorSpec`, enabling automatic IntegrityError matching and domain error conversion in `execute_creator()`, `execute_bulk_creator()`, and `execute_bulk_creator_partial()`.
