Fix autoscaling issue where deployments in ready state would not scale when their desired replica count is different from the number of their routes. Deployments now properly transition to scaling state and adjust their replicas as expected.
