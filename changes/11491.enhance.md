Add `AppProxyClientRegistry` to expose deployment-runtime domain clients (currently `DeploymentChatClient`) via the same lazy `@cached_property` pattern as `BackendAIClientRegistry`. `DeploymentChatClient` now composes a `BackendAIAppProxyClient` instead of subclassing it.
