Answer a read that already names a parent entity at that parent's scope, so a deployment's owner reaches its own routes and auto-scaling rules, and the role, resource-group and object-storage reads below them no longer ask for super-admin.
