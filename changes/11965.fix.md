Fix four v2 client methods (`SessionClient.download_single`, `InfraClient.get_watcher_status`, `get_usage_per_period`, `get_usage_per_month`) sending their parameters in the request body instead of the query string, which caused the manager to drop them (failing required-field validation). Also sign the URL-encoded query string in the client so HMAC signatures match the wire, including list values encoded as repeated query keys.
