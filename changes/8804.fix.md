Fix model service dry run failing when extra mounts are present by correctly extracting `folder_id` from `VFolderID` objects. Previously, the full `VFolderID` object was passed instead of the UUID, causing type mismatches in mount configuration.
