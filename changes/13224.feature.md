Provision every user (admin create, bulk create, signup) through a single RBAC op that creates the row with its virtual scope and own-scope roles, wires up the default keypair, and enrolls the user in its domain, project, and model-store scopes in one transaction.
