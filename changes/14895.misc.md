Read `containerRegistryImagesV2` and `imageScopedAliases` in GraphQL within the scope each one names, so a regular user who can read the registry or the image is no longer refused by the superadmin gate.
