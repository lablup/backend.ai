Pin `aiohttp~=3.13.3` in the pytest resolve so it matches the production resolve; otherwise `pytest-aiohttp` floats aiohttp to 3.14.x, whose `ClientResponse` requires a `stream_writer` argument that `aioresponses` does not pass, breaking mocked HTTP tests.
