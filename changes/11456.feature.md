Add `vfolder:data`, `session:app`, and `user:name` RBAC element types as sub-entities of vfolder, session, and user, enabling fine-grained permission control over vfolder internal data, session app endpoints, and user identity exposure separately from their parent entities.
