Add `vfolder:data`, `session:app`, and `user:name` RBAC element types so admins can be granted entity metadata access without implicit access to internal data, app endpoints, or user identity discovery.
