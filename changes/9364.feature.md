Add Prometheus query preset DB schema and ORM Row model (BEP-1050). This lays the foundation for the preset-based Prometheus query system, enabling administrators to manage reusable metric query templates.
