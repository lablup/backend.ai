Register the entities every user may read in the `public` scope, so an id read or a lookup of one passes: the types that are public as a whole, a global container registry with the images scanned from it, and a resource preset bound to no resource group.
