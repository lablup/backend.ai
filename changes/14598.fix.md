Page the REST image search with a cursor: first/after and last/before are read rather than dropped. Naming a cursor beside a limit or offset is now refused, and the default page size follows the shared value of 10 instead of 50.
