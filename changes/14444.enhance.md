Provision and tear down the virtual entity node with the row on every entity create and delete that bypassed the ops write paths, and remove the deprecated repository write specs and the provider that ran them.
