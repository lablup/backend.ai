Wire the bulk role-permission REST/GQL endpoints (`bulk-add`, `bulk-remove`, `replace`) through to the permission-controller processor so they actually mutate state, and add `scope_id` to the top-level `PermissionFilter` for symmetry with `PermissionNestedFilter`.
