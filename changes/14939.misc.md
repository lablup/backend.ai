Raise the local `pantsd_max_memory_usage` to 6GiB. A repo-wide goal exceeds the 4GiB default and restarts pantsd mid-write to the machine-wide LMDB store that every checkout shares, leaving pants unusable in all of them until the store is rebuilt. CI keeps the previous default.
