Add the `login_client_types` database table and REST v2 CRUD API for managing login client types. The `core`, `webui`, and `fasttrack` rows are seeded automatically; super admins can register additional client types via the new endpoints. Clients pass their registered client type name when logging in, and the per-user `max_concurrent_logins` limit is enforced independently per client type.
