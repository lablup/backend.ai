Add per-client-type concurrent login session tracking. Clients now send their type (core, webui, fasttrack) during login, and the per-user `max_concurrent_logins` limit is enforced independently per client type.
