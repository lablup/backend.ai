Add `used_by` to image searches to narrow results to the images a session or deployment uses, and open filters and orders for the image's namespace, registry, project, tag, config digest, accelerators, size, local-only status, type and creation time
