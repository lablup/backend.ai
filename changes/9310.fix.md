Fix session CSV export returning empty data rows by removing redundant `main_kernel_*` fields and `MAIN_KERNEL_JOIN`; add `kernel_architecture`, `kernel_registry`, `kernel_tag` to `kernel_*` fields instead.
