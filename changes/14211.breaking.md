Count API requests in a per-user fixed window (one hash per user) instead of a sorted set member per request, so rate limit memory no longer grows with request volume, and add the X-RateLimit-Reset header.
