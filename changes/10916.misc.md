Collapse session repository and action signatures to use `owner_id: UUID` instead of `owner_access_key: AccessKey`; resolve the keypair `access_key` from the owner's `main_access_key` at the use site. No schema or external behavior change.
