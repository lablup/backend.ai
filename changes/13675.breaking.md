Switch Prometheus query preset templates to `${labels}`, `${window}` and `${group_by}` placeholders — every other character is literal PromQL — and migrate stored presets automatically; the legacy `str.format` placeholder syntax is no longer accepted
