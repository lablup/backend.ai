`id` on the v2 domain API (REST `DomainNode`, GraphQL `DomainV2`) now carries the domain uuid instead of the domain name; read the name from `basic_info.name`. Domains can also be filtered by `id` (uuid) on search.
