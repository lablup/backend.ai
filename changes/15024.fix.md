Key a registered service on the address it is scraped at. The previous identity used the announced address, which has a host-independent default, so a deployment that left it unset collapsed every host of a component into one registry entry and stopped scraping the rest.
