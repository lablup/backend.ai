Reduce the manager test/typecheck re-run scope by moving api-layer primitives (`Undefined`/`undefined`, `ManagerStatus`, `BaseFilterAdapter`) down to the data layer, removing `services`→`api` cross-layer import violations.
