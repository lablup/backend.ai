Add error handling and eager iterator materialization to `_report_all_kernel_commit_status_map`. This prevents file descriptor leaks from unclosed scandir generators on exception paths and ensures errors are logged instead of silently swallowed.
