Add `app_config_fragment` bulk repository operations (`bulk_create`/`bulk_update`/`bulk_purge` with per-item partial success) on the conditional-bulk primitives, with scope-based fragment rank and the `AppConfigFragmentBulkWriteResult` data type.
