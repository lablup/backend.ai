Fix PACKAGE-mode install end to end: the manager schema was never initialized before fixtures were loaded, the supergraph download URL used a non-existent `v`-prefixed release tag, and `PackageContext` left the app-proxy secrets unset, which raised a TOML serialization error while writing the app-proxy configs.
