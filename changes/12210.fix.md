Fix PACKAGE-mode install failures: the supergraph download URL used a non-existent `v`-prefixed release tag, and `PackageContext` left the app-proxy secrets unset, which raised a TOML serialization error while writing the app-proxy configs.
