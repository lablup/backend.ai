Load kernel resource allocations in the GraphQL DataLoader through a partial bulk field read, so a batch mixing readable and unreadable kernels answers the readable ones instead of failing every `resource_allocation` field in it.
