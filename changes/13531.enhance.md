Record which keypair is a user's main one on `keypairs` alone, dropping the `users.main_access_key` column that an unrelated keypair's deletion could clear; the `main_access_key` API field, its update path and its filters are unchanged and read the marker
