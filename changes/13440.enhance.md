Type the REST authentication context as the `AuthenticatedUser` and `AuthenticatedKeypair` dataclasses, so authenticating a request loads only the `users` and `keypairs` columns that request handling reads instead of every column of both rows.
