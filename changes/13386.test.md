Add component tests for the app config REST v2 routes: the merged read's per-scope visibility and merge rules over a real server and database, and the superadmin gating and CRUD round trips of the definition and allow-list routes.
