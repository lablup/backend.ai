Register `ImageRow` in the scaling group service test conftest so the CRUD tests pass when the target is run in isolation (the string-based `KernelRow.image_row` relationship needs `ImageRow` in the SQLAlchemy registry at mapper-configuration time).
