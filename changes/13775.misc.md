Gather the confidential-computing auxiliary trees into `cc/` subdirectories of the matching top-level directories (`scripts/cc`, `configs/cc`, `docker/cc`) and promote the credential broker to a regular component at `src/ai/backend/cc_broker`
