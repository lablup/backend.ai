Compare an existing ESP security association's key material before adopting it on recovery: the SPI is derived from the VTEP pair and the generation slot alone, so an association two rotations old sits at the same identity carrying a key nobody uses, and adopting it on identity would have kept a retired key protecting live traffic. The periodic protection pass now also restores a session's bridge forward-accept rule, which was installed once and never re-checked. The pass also takes back a bridge forward-accept rule left on a VNI this node has no tunnel for, which is what a process that died between writing the rule and registering the session leaves behind. A privnet-backed node stops heartbeating -- and announces itself as restarting -- while it cannot reach its privileged network helper, since every session's devices on such a node are made by that process; it is announced again by the first heartbeat after the helper is back. A gossip receiver now takes an announcement by the fields it understands rather than refusing one whose version number is not its own, so a node upgraded ahead of its peers keeps exchanging endpoints with them.
