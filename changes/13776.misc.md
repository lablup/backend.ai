Add confidential computing: attested guest sessions, a measured control plane with an attestation-gated credential broker at `src/ai/backend/cc_broker`, the `backend.ai/cc-storage/v1` encrypted storage format, and the auxiliary build/provisioning trees under `scripts/cc`, `configs/cc`, and `docker/cc`
