Reclassify the entity types that name no row of their own — `auth`, `etcd_config`, `manager_admin`, `secret` and the three fair share kinds — as field kinds, so their operations no longer claim an entity that does not exist.
