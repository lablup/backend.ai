Propagate the `triggered_user()` identity through the event dispatcher so background handlers restore both the effective and triggering user, keeping audit consistency for work triggered during impersonation (BEP-1058)
