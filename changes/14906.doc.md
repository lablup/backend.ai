Add BEP-1079, the VXLAN overlay cluster network: the manager allocates each multi-node session a subnet and a VNI, and each agent builds its node's half of the overlay through a runtime-neutral seam, with an optional privileged helper so the agent itself needs no CAP_NET_ADMIN.
