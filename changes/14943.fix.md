Detect lost agents on the leader instead of in every manager worker. Each worker ran the check on its own and fired a termination event per lost agent, so a single lost agent produced one event per worker every second.
