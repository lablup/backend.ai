Prevent inference deployments with a null `current_revision` from wedging in SCALING, and let `model-card update` actually sync `min_resource` (which previously silently dropped the new values and returned a misleading 404).
