Add database migration to integrate Notification entities (channels and rules) into the RBAC system. All notification channels and rules are associated with user scopes based on their creator, with appropriate permissions assigned to roles within those user scopes.
