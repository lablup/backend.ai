Add RBAC Role read and list REST API endpoints for RBAC management. Introduces GET /roles/{role_id} for retrieving specific role details and POST /roles/search for searching roles with filtering, sorting, and pagination support.
