Move the trailing model card, retention, vfolder and replica group write specs onto the v2 spec lineage, so every repository in those domains takes a DB ops provider instead of building ops or holding an engine.
