Keep project membership within a single domain: refuse to bind a user to a project in another domain when granting a project-scoped role, and re-sync a user's memberships when it is moved to another domain.
