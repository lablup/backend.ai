Add `VFolderOwnershipInfo` nested type to VFolder GQL schema, grouping scalar ID fields (`userId`, `projectId`, `creatorEmail`) and node resolvers (`user`, `project`, `creator`) under a single `ownership` field.
