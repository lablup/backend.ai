Enhance object permission assignment to maintain cross-scope access consistency. When adding object permissions to a Role through operations like invite/sharing, the system now automatically updates the role's scope bindings to include any cross-scope entities referenced by the new permissions
