Prune dead model serving code: the unused `create_model_service` and `search_auto_scaling_rules` action chains plus the stale `ModelServiceCreator`, `RouteConnectionInfo`, and `RequesterCtx` data containers.
