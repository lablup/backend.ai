Remove unused model serving code: the `create_model_service` action chain along with `ModelServiceCreator`, the `search_auto_scaling_rules` chain (superseded by the deployment service), and the stale `RouteConnectionInfo` / `RequesterCtx` data containers.
