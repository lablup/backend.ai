Fix `backend.ai session ssh` failing with a `FileNotFoundError` when several invocations run concurrently from the same working directory, and stop it from writing the downloaded container SSH key into the current directory
