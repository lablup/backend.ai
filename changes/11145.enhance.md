Unify deployment revision merge pipeline so all sources (deployment-config.yaml, revision preset, model-definition.yaml, request) are converted to RevisionDraft and merged by DeploymentController in a single pass.
