Add --with-harbor option to the dev-mode TUI installer that installs a local Harbor container registry via the official offline installer, configured through ruamel.yaml round-trip editing. Manage lifecycle with ./dev harbor start/stop.
