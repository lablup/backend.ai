Stop manager repositories from importing the api layer (move Valkey statistics loaders into a client module and drop unused filter/order wrappers) so editing api modules no longer expands the repository test/typecheck scope.
