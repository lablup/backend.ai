Register a persistent v2 `BackendAIAnonymousClient` on the webserver app (`app["anon_client"]`) and extract `build_forwarding_headers()` as a reusable pure function for building proxy forwarding headers.
