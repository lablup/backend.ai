Fix sessions getting stuck in `TERMINATING` when they run on agents whose `schedulable` flag is `false`. Session status promotions now run on all scaling groups with ALIVE agents regardless of the `schedulable` flag.
