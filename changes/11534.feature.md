Route coordinator now scans lifecycle routes via `BatchQuerier` with pagination, and accepts an explicit traffic-status filter axis so handlers can target only routes whose `traffic_status` is in a given list.
