Fix the `createAccessToken` GraphQL mutation always failing with `deployment_id Field required` by renaming the `CreateAccessTokenInput` GQL field `modelDeploymentId` to `deploymentId` to match its DTO.
