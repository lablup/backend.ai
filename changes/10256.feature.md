Add Pydantic DTO v2 models for `scaling_group`, `resource_group`, `resource_slot`, and `resource_policy` domains under `common/dto/manager/v2/`, including request (Input), response (Node/Payload) models with SENTINEL pattern, and comprehensive unit tests for all four domains.
