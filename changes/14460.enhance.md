Record every authentication operation against the user it reaches, resolving a request's user scope and access-key scope as lookups that answer with that user, and drop the `auth` entity kind that named no row.
