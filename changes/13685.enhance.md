Add `service-launch-timeout-sec` to the agent's `[kernel-lifecycles]` config so operators can tune how long a service app (jupyter, vscode, ...) may take to open its port; the agent and the kernel runner now derive their timeouts from this single value.
