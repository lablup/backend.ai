Expose the `login_client_types` entity via the Strawberry GraphQL schema: `loginClientType(id)` single query, `loginClientTypes` Connection query with filter/order/pagination, and `createLoginClientType` / `updateLoginClientType` / `deleteLoginClientType` mutations.
