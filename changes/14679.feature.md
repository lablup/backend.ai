Separate the vfolder mount level from RBAC access: a folder carries a default mount permission and per-user mount policies, settable through the v2 REST, GraphQL, SDK and CLI, and the `vfolder_permissions` and `vfolder_invitations` tables are dropped.
