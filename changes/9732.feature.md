Add repository, data, and service layer for domain/project active resource overview, including `ResourceOccupancy` dataclass, SQL aggregation methods with active-status filters, and `GetDomain/ProjectResourceOverview` Action/ActionResult pairs registered in `ResourceSlotProcessors`.
