Rename `UserPermission.user_uuid` to `owner_id` and add `main_access_key` field. `KernelRow.to_kernel_info` populates `main_access_key` from the eagerly-loaded `user_row` relationship so downstream consumers no longer need to pass the access_key through separate channels.
