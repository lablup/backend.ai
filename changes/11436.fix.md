Restore resource group auto-selection at session enqueue when the caller omits `--scaling-group`/`-q`. The first allowed resource group is now picked, instead of failing with `SessionSpec fields not resolved: scope.resource_group_name`.
