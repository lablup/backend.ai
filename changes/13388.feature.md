Let a browser read public data before it holds credentials: public GraphQL fields now come from a `public` subgraph of the same supergraph, reached anonymously at `/func/admin/gql`, and `/v2/{entity}/public/{operation}` is proxied without credentials.
