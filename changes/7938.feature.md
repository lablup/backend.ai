Introduce `StorageTarget` abstraction for storage step mappings in `storage-proxy`, enabling more flexible configuration of storage backends and improving extensibility for future storage-related features
