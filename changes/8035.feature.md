Apply `RBACFieldCreator` pattern to Kernel creation, establishing Session-Kernel relationship via `EntityFieldRow`. Add `FieldType` enum to distinguish field-scoped entities from scope-scoped entities.
