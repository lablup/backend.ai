Source session/kernel `access_key` responses (GraphQL, REST, event payloads, reports) from the owner's `main_access_key` via a `UserRow` join instead of reading `SessionRow.access_key` / `KernelRow.access_key` directly. External wire schemas are preserved.
