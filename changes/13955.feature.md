Open `restore` on domains, projects, users, images, and vfolders, refuse writes while a purge is in progress, and block a soft-deleted user at API authentication instead of deactivating their keypairs.
