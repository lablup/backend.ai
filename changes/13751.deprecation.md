Add `updated_at` to the domain v2 REST and GraphQL APIs and deprecate `modified_at`, which keeps reporting the same value. The `domains.modified_at` column itself is renamed to `updated_at` via the shared lifecycle timestamp mixins, and the legacy GraphQL `Domain` and `DomainNode` fields are unchanged.
