Add BEP-1062 proposing the Virtual Scope RBAC ownership model that replaces the recursive scope-walk with a non-recursive two-hop `scope -> virtual_scope -> entity` resolution, documenting the as-built tables/ops and the migration plan.
