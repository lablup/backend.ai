Eagerly load `EndpointRow.revisions` in `ModelServingRepository.update_route_traffic` (and other `to_data()` call sites) so updating a route's traffic ratio no longer raises `sqlalchemy.exc.MissingGreenlet` when the endpoint owns a deployment revision.
