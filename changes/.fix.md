Convert remaining `BraceStyleAdapter` f-string log calls across accelerator, agent, appproxy, common, kernel, manager, and storage modules to lazy brace-style formatting to prevent secondary `.format()` failures on values containing literal brace characters.
