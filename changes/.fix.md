Surface `reads_vfolder_config_files` and `default_model_definition` in the runtime variant CRUD stack (REST v2, GraphQL, SDK, CLI) and fix the broken create path that was rejecting inserts on the NOT NULL column.
