Derive distributed training environment variables (`WORLD_SIZE`, `RANK`, `LOCAL_RANK`, `MASTER_ADDR`, `MASTER_PORT`, `TF_CONFIG`) from existing `BACKENDAI_*` cluster variables at container startup for PyTorch and TensorFlow
