Rename the `domains.modified_at` column to `updated_at` and take it from the shared lifecycle timestamp mixins, tightening both domain timestamps to NOT NULL on databases old enough to still allow NULLs; the API keeps reporting the value as `modified_at`.
