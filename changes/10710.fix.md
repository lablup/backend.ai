Fix `validate_ip` to split `X-Forwarded-For` header by comma and use the first IP, consistent with `myClientIp` and `get_my_ip`, preventing allowed client IP validation failures in proxy/LB environments.
