Fix client IP resolution inconsistency between `validate_ip` and `myClientIp` in proxy/LB environments, and add optional `XForwardedStrict` middleware support via `trusted_proxies` config for secure X-Forwarded-For validation.
