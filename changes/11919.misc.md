Replace Pants' experimental visibility linter with a standalone Rust `visibility-checker` that enforces `visibility_private_component()` BUILD rules across transitive import paths; prebuilt binaries for Linux x86-64, Linux aarch64, and macOS aarch64 are shipped as Git LFS objects.