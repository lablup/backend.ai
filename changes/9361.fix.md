Introduce `DeploymentLifecycleStatus` and `DeploymentSubStatus` types to deployment handler interface, replacing raw `EndpointLifecycle` returns with structured transition targets that can carry sub-status context