Fix `DeploymentOrders.updated_at` crash by removing the non-existent column reference, replace it with `DESTROYED_AT`, and extend deployment / revision search APIs with commonly needed filters such as `domain_name`, `project_id`, `resource_group`, `created_user_id`, `session_owner_id`, `created_at`, `destroyed_at`, `image_id`, `model_id`, `cluster_mode`, and `runtime_variant`.
