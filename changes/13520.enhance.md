Make semantically required lifecycle timestamp columns (`created_at` / `updated_at`) non-nullable via shared timestamp mixins, renaming the legacy `modified_at` columns of users / keypairs / groups / vfolder_invitations to `updated_at`.
