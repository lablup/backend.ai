Populate revision-level fields on the legacy GQL endpoint response during the initial DEPLOYING phase by falling back to `deploying_revision` when `current_revision` is unset, expose `resource_slots` on the v2 revision response, and stop hard-coding cluster mode / size / runtime variant in the model-card and vfolder `deploy` adapters so the revision preset's values are no longer silently overridden.
