Fix a manager memory leak where the background-task event stream (`GET /events/background-task`) leaked an asyncio task and a registered event propagator when a client disconnected before the task finished.
