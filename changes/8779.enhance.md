Allow RBAC GQL type declarations and fetcher implementations to be developed independently by making `build_connection()` work with `SearchResult[T]` and removing field duplication across search result layers
