Fix MissingGreenlet error in AgentDBSource.get_by_id by using joinedload instead of selectinload. This ensures the kernels relationship is properly loaded in async context, preventing lazy loading errors when computing actual_occupied_slots.
