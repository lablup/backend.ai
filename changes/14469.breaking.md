A permission row no longer carries a scope: the role it belongs to places every grant, and `scopeType` / `entityType` on the RBAC API carry the entity type name instead of the `RBACElementType` enum.
