Add BEP-1052 proposing a scoped redesign of the WebUI App Config API: per-scope `AppConfigSource` rows (public/domain/domain_user_defaults/user) joined by a required `AppConfigPolicy` that drives the per-document merge chain into the merged `AppConfig` view (admins can resolve any user's view via `adminAppConfigs`); bulk-only writes split across admin / self-service / policy paths; REST list endpoints unified under `POST .../search` with paginated bodies.
