Add BEP-1052 proposing a scoped redesign of the WebUI App Config API (public/domain/domain_user_defaults/user scopes, named documents, soft-delete + restore, 2-way merge exposed via a separate `MyAppConfig` type, bulk-only admin and self-service write mutations).
