`entityTypes` lists the entity types a request may name, read from the operations the manager has wired. The same list is served over REST (`GET /v2/entity-types`), the v2 SDK (`registry.entity_type.list()`), and the CLI (`./bai entity-type list`).
