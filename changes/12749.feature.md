Add a `traefik` app-proxy frontend mode to the TUI installer (`backend.ai install --frontend-mode=traefik`): Traefik runs as a halfstack compose service (profile `traefik`) with the appproxy Traefik plugin, and the coordinator/worker configurations are generated accordingly.
