Fix TUS resumable upload `409 Conflict` errors behind multi-replica load balancers on NFS storage by coordinating per-session upload offsets through Valkey (single source of truth) and serializing the write-and-commit critical section with a Sokovan-style TTL'd lease, eliminating reliance on NFS-cached `stat()` results that diverge across storage-proxy replicas.
