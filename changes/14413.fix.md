Open the `installed` field of the legacy `ImageNode` to every authenticated caller: the `get_image_installed_agents` read behind it was gated superadmin-only by the v2 processor migration, although the resolver reduces it to a boolean and no agent name leaves the manager through it.
