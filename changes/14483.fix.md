Adopt the ESP security associations the kernel already holds instead of replacing them: which key generation a slot holds lived only in the agent's memory, so a restart rewrote SAs that were already correct, reset their replay state, and the peer dropped everything the node sent as a replay until its sequence climbed past the window.
