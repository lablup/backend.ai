Add the merged AppConfig GraphQL read: `myAppConfigs` for the acting user and `publicAppConfigs` for anonymous pre-login callers, which also re-enables the unauthenticated public GraphQL endpoint (BEP-1052).
