Fix the `OSV-Scanner` workflow's `scan-containers` matrix by passing the `PYTHON_VERSION` build-arg to `docker/build-push-action`, switching to OSV-Scanner v2's `scan image --archive` syntax with explicit `--output`, and guarding the SARIF upload (bumped to `github/codeql-action/upload-sarif@v4`); the same `PYTHON_VERSION` build-arg fix is also applied to `sbom.yml`.
