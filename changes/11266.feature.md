Add `app_config_fragments` and `app_config_policies` tables (per BEP-1052 §1) — per-scope fragment rows keyed by `(scope_type, scope_id, name)` and matching policy rows that drive the merge order. Internal-only at this point; the GraphQL / REST surface lands in subsequent PRs.
