Audit logs now record which entities and scopes an action affected, log rejected actions with a `denied` status, and report bulk results per entity; recording successful read operations is opt-in via `[audit-log] record-read-operations`.
