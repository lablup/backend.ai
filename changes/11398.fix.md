Replace the native `sessionresult`/`sessionresults` PostgreSQL ENUM types backing `kernels.result` and `sessions.result` with `VARCHAR(64)` + `StrEnumType` so that `alembic upgrade` is no longer fragile to diverged enum-type names across environments.
