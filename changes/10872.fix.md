Fix stuck session termination and promotion flows by running coordinator checks across all resource groups and allowing explicit termination paths to handle pending, deprioritizing, and already terminating sessions correctly.
