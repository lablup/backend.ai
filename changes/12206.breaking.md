The TUI installer no longer hardcodes the wildcard app/storage base domains; installs that pass `--fqdn-prefix` must now also pass `--app-base-domain` and `--storage-base-domain`, otherwise those hosts fall back to `--public-facing-address`.
