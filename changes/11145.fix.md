Populate `deployment_revisions.model_definition` for legacy endpoints (`POST /func/services`) by unifying the revision merge pipeline so all sources — `deployment-config.yaml`, revision preset, `model-definition.yaml`, and request — flow through `RevisionDraft`.
