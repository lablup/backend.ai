Deprecate the resource group's `enforce_spreading_endpoint_replica` scheduler option. Nothing reads it anymore — replica spreading is now governed by the replica group's SessionGroup placement policy, and existing values were migrated onto it. The field stays in the schema and will be removed in the next major release.
