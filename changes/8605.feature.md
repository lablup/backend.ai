Register Traefik /metrics endpoint with Service Discovery when AppProxy Worker starts in Traefik mode. This enables Prometheus to automatically discover and scrape Traefik's internal metrics (request counts, latencies, etc.) via the existing HTTP SD mechanism
