Proxy the manager's anonymous reads (`/admin/gql/strawberry/public` and `/v2/{entity}/public/{operation}`) through the webserver, which previously answered `401` before the request reached the manager.
