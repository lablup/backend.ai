Let the anonymous manager reads — the public GraphQL endpoint (`/admin/gql/strawberry/public`) and any `/v2/{entity}/public/{operation}` path, which currently means the merged app config view — through the webserver, which previously answered 401 for them before proxying to the manager.
