Make `id` on the v2 domain API (REST `DomainNode`, GraphQL `DomainV2`) the domain uuid — the name lives at `basic_info.name` — with an `id` search filter, and expose `entityId` on `DomainV2` as a plain `UUID` scalar readable without decoding the Relay global id.
