Expose the domain uuid as `row_id` on the v2 domain API (REST, GraphQL `rowId`, and the CLI payload), so domain-scoped APIs such as app config fragments can be addressed without reading the uuid from the database.
