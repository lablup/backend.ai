Model message queue and event payloads with Pydantic — splitting anycast and broadcast into independent message types and making every event a Pydantic model that renders itself as a single `EventMessage` — ahead of the JSON event serialization cutover
