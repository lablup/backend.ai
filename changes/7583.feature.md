Add database migration to integrate App Config entities into the RBAC system. Each app config is associated with its respective scope (domain, project, or user) based on its scope type and ID, with appropriate permissions assigned to roles within those scopes.
