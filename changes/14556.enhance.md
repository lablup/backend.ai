Validate the container registry URL and the Harbor project name on create and update through the REST v2, GraphQL v2 and legacy GraphQL APIs alike; an invalid value is now refused before anything is written.
