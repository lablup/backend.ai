Refuse an admin-only GraphQL call with `InsufficientPrivilege`. The refusal previously raised an aiohttp exception the GraphQL handler did not recognise as a client error, so an ordinary permission check was logged as an unexpected error with a stack trace.
