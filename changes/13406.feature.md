Resolve container registry ownership through the virtual-scope chain: the registry's virtual scope is created and deleted with the registry, and allowed projects are bound to it so project-scoped permissions reach the registry's entities
