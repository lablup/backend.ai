Expose the model deployment a session serves on the `SessionV2` GraphQL type: add a `replica` field (backed by a new nullable `sessions.replica_id` column populated from route bindings) plus `deploymentId`/`deployment` on `ModelReplica`, completing the session → replica → deployment traversal.
