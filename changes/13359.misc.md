Drive `scripts/assign-pr-number.py` entirely from its arguments: it takes the base ref as an option, prints the renames it made as JSON, and no longer reads `GITHUB_BASE_REF` or writes `$GITHUB_OUTPUT`.
