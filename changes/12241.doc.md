Rewrite BEP-1052 (Scoped App Config Redesign) to match the rank-based implementation — drop the `AppConfigPolicy` / `scope_sources` design, remove the code/GraphQL/REST/caching detail, and trim it to ideas only.
