Fix session creation failure caused by mismatched PostgreSQL enum type name by adding an Alembic migration to unify the enum type name from `sessionresult` (singular, from 2019) to `sessionresults` (plural, matching the 2022 migration and ORM code).
