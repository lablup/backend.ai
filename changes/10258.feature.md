Add Pydantic DTO v2 models for `session`, `compute_session`, and `agent` domains under `src/ai/backend/common/dto/manager/v2/`, following the Input/Node/Payload naming convention with nested sub-models for semantic field grouping.
