Require `resource_slots`, `cluster_mode`, `cluster_size`, `replica_count`, and `deployment_strategy` when creating a `DeploymentRevisionPreset` so deployments built from it no longer silently fall back to system defaults.
