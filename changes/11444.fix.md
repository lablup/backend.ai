Reject deployment-revision creation when no source supplied `resource_slots` so deployments can no longer be built with empty resources, and require `cluster_mode` / `cluster_size` when creating a `DeploymentRevisionPreset`.
