Require every value needed to create a deployment when creating a `DeploymentRevisionPreset` (`resource_slots`, `cluster_mode`, `cluster_size`, `replica_count`, `deployment_strategy`). Existing rows are migrated with safe defaults (`replica_count=1`, `deployment_strategy=ROLLING`).
