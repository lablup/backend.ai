Rename `sessions.user_uuid` / `kernels.user_uuid` columns to `owner_id` and drop the `sessions.access_key` / `kernels.access_key` columns. The downgrade path is lossy: `access_key` is recreated as nullable without backfill.
