The agent config key `scaling-group` is replaced by a nullable `initial-resource-group-name` that only seeds the resource group at the agent's first registration; agents with the removed key in `agent.toml` now fail at startup, and when unset the manager assigns the default resource group.
