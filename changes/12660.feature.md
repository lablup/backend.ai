Add a `triggered_user()` identity context and resolve the `X-BackendAI-Act-As` header in the REST auth middleware, letting a super admin run a request under a target user's permission context (BEP-1058)
