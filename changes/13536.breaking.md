Change the GraphQL `model_definition` types of deployment revision presets: the update mutation input moves from `ModelDefinitionInput` to `PresetModelDefinitionInput` (each model now requires a `service` block), and the preset output moves from `ModelDefinition` to a new `PresetModelDefinition` type with nullable `name`/`model_path`/`port`
