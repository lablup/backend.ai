Allow deployment revision presets to omit model `name`, `model_path`, and service `port` so revisions inherit merge-chain defaults at resolution, moving the preset GraphQL `model_definition` input/output to sparse `PresetModelDefinitionInput`/`PresetModelDefinition` types
