Refactor ValkeyClient operations to use an async context manager pattern that tracks consecutive operation failures and triggers reconnection via the monitor loop when the failure threshold is exceeded
