Refactor ValkeyClient operations to use an async with acquire() context manager that tracks consecutive connection failures and tears down the connection when the threshold is exceeded, instead of continuing to retry on a broken connection
