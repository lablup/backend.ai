Remove the unused PyInfra production-deployment scaffold under `src/ai/backend/install/pyinfra/` and clean up stale pants build configuration so `pants lint`, `check`, and `tailor --check` report no warnings.
