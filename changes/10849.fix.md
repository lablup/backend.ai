Fix `alembic downgrade` across `04e150fdefa0` failing with `DatatypeMismatchError` on `routings.traffic_status` by dropping the VARCHAR default before recasting the column back to the `routetrafficstatus` enum.
