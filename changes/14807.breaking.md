Remove the access token value from the deployment access token search input. The token value is the credential itself, so it is no longer accepted as a search condition; filter by expiry or creation time instead.
