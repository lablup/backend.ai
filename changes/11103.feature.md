Add admin REST v2 endpoint, GraphQL mutation, SDK method, and CLI command to sync `model_definition` from vfolder storage for deployment revisions where the stored value is NULL or differs from the vfolder file content.
