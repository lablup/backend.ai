Give a force-terminated session's volatile network back: the forced path writes TERMINATED directly and so never reached the hook that releases it, leaving an overlay session's VNI, subnet, endpoints and IP reservations claimed in etcd for good.
