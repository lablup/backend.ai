Let a user read the keypair resource policy that applies to their own keypair: the legacy GQL `keypair_resource_policy(name:)` / `user_resource_policy(name:)` queries and the v2 `my_keypair_resource_policy` reads no longer refuse regular users, and the `user_owner` role now holds `keypair_resource_policy:read`.
