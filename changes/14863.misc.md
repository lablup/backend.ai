Resolve resource policy names for every authenticated caller, as domain and resource group names are, so the legacy GQL `keypair_resource_policy(name:)` and `user_resource_policy(name:)` queries no longer refuse regular users; and grant the `user_owner` role `keypair_resource_policy:read` so `my_keypair_resource_policy` answers for them too.
