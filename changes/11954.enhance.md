Allow a vfolder referenced by UUID to be mounted at multiple distinct subpaths within a single session. A UUID-referenced mount request that resolves to no accessible vfolder now raises `VFolderNotFound` instead of being silently dropped.
