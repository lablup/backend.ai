Store the keypair secret key encrypted at rest. Existing rows stay plaintext and keep working; a new keypair is encrypted once `secret-encryption.write-provider-type` names a key provider. Deploy while the write provider is `plain`, then configure keys and name the provider — rolling back to an older build after that breaks authentication, because ciphertext is read as plaintext, so every stored value must be plaintext again before reverting.
