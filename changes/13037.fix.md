Gate the app config allow-list, definition and fragment admin searches on the SUPERADMIN role at the action layer by basing them on `BaseGlobalAction`, replacing the stub GLOBAL scope with an empty element id that no scope chain resolved.
