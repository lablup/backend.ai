Add BEP-1072 proposing release branch isolation and backport automation: cut the version branch from the rc1 release on `main`, decide backport targets by PR title prefix and labels, leave failed backports as draft PRs, and split the changelog into per-version-branch files.
