Give a registered service an identifier derived from its endpoint. A component's worker processes share one listening socket, so per-process identifiers registered the same endpoint once per worker, and a restart left the old entries behind until the sweep cleared them.
