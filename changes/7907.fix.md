Add unique constraints to RBAC permission tables (`object_permissions`, `permission_groups`) to ensure idempotent behavior. The migration also removes any existing duplicate records before applying constraints.
