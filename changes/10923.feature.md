Add the `login_client_type` service layer, v2 DTOs, and an admin-only search path (`LoginClientTypeAdminRepository` / `LoginClientTypeAdminService` / `LoginClientTypeAdminProcessors`) with filtering, ordering, and pagination support via `BatchQuerier`.
