Fix four v2 client methods (`SessionClient.download_single`, `InfraClient.get_watcher_status`, `get_usage_per_period`, `get_usage_per_month`) sending their parameters in the request body instead of the query string, which caused the manager to drop them (failing required-field validation). Also sign the URL-encoded query string across all v2 client transports (`_request`, `download`, `upload`, `ws_connect`, `sse_connect`) so HMAC signatures match the wire — fixing signature-validation failures for SSE/WebSocket streaming calls that pass query params — including list values encoded as repeated query keys.
