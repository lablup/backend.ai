 Add `OptionalType` class to take the original type parameter (paramtype class or instance) and allow passing the `undefined` value as-is while enforcing the original logic in other cases. 
 Include minor fixes.