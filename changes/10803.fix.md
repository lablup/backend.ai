Restrict `agent_nodes` and `agents_v2` GraphQL queries to superadmin only, preventing regular users from accessing agent infrastructure details such as internal addresses, resource capacity, and hardware metadata.
