Project searches now require a single member to satisfy every condition under the `user` filter, deprecate the nested `domain` and `user` filters, and accept description and integration name as filters and project ID as an order.
