Source the advertised API version and manager version in the docs from `ai.backend.client.config.API_VERSION` and the `VERSION` file via the `|latest_api_version|` and `|release|` Sphinx substitutions, so the homepage and the manager install guide no longer drift from the actual versions.
