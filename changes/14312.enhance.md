Replace the request-DTO `Sentinel` enum with pydantic's `MISSING` behind a `common/tristate` `Unset` alias, so an unsent update field carries no placeholder value on the wire or in the OpenAPI schema
