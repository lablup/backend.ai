Fix v2 client `InfraClient.get_wsproxy_version` sending its `group` filter in the GET request body instead of the query string, which caused the manager to silently drop it so the wsproxy-version lookup was not scoped to the requested group.
