Add `service.instance.id` and `service.instance.name` OpenTelemetry resource attributes to enable per-instance log filtering in Loki/Grafana. The `service.instance.id` is generated using UUID v5 from the instance name for stability across restarts, following OpenTelemetry semantic conventions.
