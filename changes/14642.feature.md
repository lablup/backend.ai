Add `value_type: "pct"` (`current / capacity × 100`) to `user_utilization_metric`, and report the container `cpu_util` capacity as the kernel's allocated cores instead of one core, so `cpu_util` utilization is the share of the allocated cores.
