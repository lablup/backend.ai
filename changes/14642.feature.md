Add `value_type: "pct"` to `user_utilization_metric`, computed by the manager as `current / capacity × 100` so clients no longer need per-metric scaling such as the cpu_util `/ 10`, and report the container `cpu_util` unit hint as `msec` (CPU time) instead of `percent`.
