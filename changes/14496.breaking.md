The auth, user and keypair errors now report the row type as the error code domain: eleven codes change, among them `keypair_read_not-found` to `user-keypair_read_not-found` and `auth_read_not-found` to `user-login-session_read_not-found`. HTTP statuses, `error_type` URLs and titles are unchanged.
