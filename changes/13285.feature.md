Expose the resource group `is_default` flag in the v2 API: readable on the GraphQL and REST v2 resource group status, filterable in search, and settable via the update-config endpoint (setting it clears the previous default in the same transaction).
