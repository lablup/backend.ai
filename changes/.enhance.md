Add ``users.domain_id`` UUID foreign key referencing ``domains.id``, retain the legacy ``users.domain_name`` string column without an FK constraint, and propagate the new identifier through ``UserCreatorSpec`` / ``UserUpdaterSpec`` and the REST / GraphQL user create / update endpoints.
