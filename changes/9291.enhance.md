Centralize duplicated error handling from stat collection methods into the `_observe_stat_task` decorator, and add distinct handling for `EMFILE` (FD exhaustion) to log a concise warning instead of a full traceback.
