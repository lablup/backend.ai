Put an overlay session's bridge forward-accept rule back when the agent adopts it after a privileged-network-helper restart: the recovery sweep removes four rules for the VNI and the protection re-assert restored three, so on a node that filters FORWARD by default the adopted session stopped carrying traffic for as long as it ran.
