Reduce the recursive working set when resolving effective permissions or running bulk permission checks for many entities sharing a direct parent scope by routing both through a shared fan-in dedup path.
