Refactor model card delete to follow the `Purger` / `Updater` repository patterns, run bulk-delete in a single transaction with per-card savepoints so a single failure no longer aborts the rest of the batch, and add a new `adminBulkDeleteModelCardsV2` mutation (and matching REST/SDK/CLI surface) that surfaces per-card failures (`failed: [{ card_id, message }]`). The existing `adminDeleteModelCardsV2` mutation is kept for backwards compatibility but deprecated in favor of the new one.
