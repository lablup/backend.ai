Refactor model card delete to follow the `Purger` / `Updater` repository patterns, and run `adminDeleteModelCardsV2` in a single transaction with per-card savepoints so a single failure no longer aborts the rest of the batch.
