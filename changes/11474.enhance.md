Refactor model card delete to follow the `Purger` / `Updater` repository patterns, run `adminDeleteModelCardsV2` in a single transaction with per-card savepoints so a single failure no longer aborts the rest of the batch, and surface per-card failure details on the bulk-delete payload (`failed: [{ card_id, message }]`) instead of only the success count.
