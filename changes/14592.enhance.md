Retire the `BaseAction` action framework, moving every remaining operation onto the v2 bases and splitting the reads that served several questions behind one action, so a role, permission or assignment the caller may not read leaves the answer instead of being served unchecked.
