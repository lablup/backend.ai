Fix `POST /session` failing with `expected str, got URL` when a `callback_url` is provided, by giving `KernelRow.callback_url` the `URLColumn` type decorator so `yarl.URL` values are coerced to strings before reaching asyncpg.
