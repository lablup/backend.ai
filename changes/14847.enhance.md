Role searches take a `used_by` narrowing to the roles a role preset was used for, and deprecate the `assigned_user` filter along with the role assignment's nested `role` and `permission` filters, which are removed in the next release. Creating a role preset is authorized as a create in the global scope instead of on the superadmin gate alone.
