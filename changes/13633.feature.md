Add the v2 write spec lineage with per-entity membership declarations, so creating, purging, or upserting a scope-membered entity registers and removes its virtual-scope membership as part of the write itself
