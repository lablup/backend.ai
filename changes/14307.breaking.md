Rate limit unauthenticated requests by client address, and resolve that address from the peer of the connection unless `trusted-proxies` names the hops in front of the manager, so a proxied deployment must configure it to keep resolving callers by `X-Forwarded-For`.
