Resolve the client IP from the peer of the connection unless `trusted-proxies` names the hops in front of the manager, so `X-Forwarded-For` no longer decides the rate limit window, the `allowed_client_ip` check or the address written to audit logs. A deployment behind a proxy must list it in `trusted-proxies` to keep resolving callers by the forwarded address.
