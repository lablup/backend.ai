Add BEP-1061 proposing a structured Agent kernel lifecycle (prepare/create/terminate) with async triggers and transition events reported to the manager, keeping the authoritative kernel status state machine in sokovan.
