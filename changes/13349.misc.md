Move the 26.4 and later release blocks from the root `CHANGELOG.md` into `CHANGELOG/<version>.md`, backfill the releases that were only ever published from a version branch, and index which feature landed in which version in `CHANGELOG/README.md`.
