Add Pydantic DTO v2 models for `artifact`, `artifact_registry`, and `container_registry` domains under `common/dto/manager/v2/`, including typed Input, Node, and Payload models with full unit test coverage.
