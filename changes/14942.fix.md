Stop Prometheus from scraping a multi-worker component once per worker. Every worker registered itself in service discovery under the same shared address, so each `sum()` over the metrics came out inflated by `num-proc`.
