Add the merged AppConfig CLI v2: `bai my app-config get` for the acting user and `bai public app-config get` for pre-login callers holding no credentials, alongside a new `public` scope group (BEP-1052).
