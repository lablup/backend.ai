Bind `user`/`domain` AppConfig fragments to their RBAC scope on write so writes can be RBAC-authorized (`public` stays global); register `APP_CONFIG_FRAGMENT` as an RBAC scope and backfill role permissions via migration (BEP-1052).
