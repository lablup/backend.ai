Bind `user`/`domain` AppConfig fragments to their RBAC scope on create and unbind them on purge, so a fragment's ownership is resolvable when authorizing writes (`public` stays global-scoped, with no association) (BEP-1052).
