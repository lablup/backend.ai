Fold the per-project registry quota service and repository into `ContainerRegistryService` and `ContainerRegistryRepository`, removing the singular `manager/service/` package and the `ServicesContext` wrapper.
