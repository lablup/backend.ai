Consolidate the singular `manager/service/` package into `manager/services/` and replace the one-field `ServicesContext` wrapper with a typed `registry_quota_service` field on the API and domain contexts.
