Add `adminDeleteModelCardsV2` bulk-delete with per-card savepoint (partial-failure) semantics — a single failure (missing card or mounted vfolder) no longer aborts the rest of the batch, and any sibling cards on the same vfolder are cascade-deleted when `delete_associated_vfolder` is set.
