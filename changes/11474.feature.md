Replace the model card bulk-delete API with `adminBulkDeleteModelCardsV2`, which deletes model cards in a single transaction and reports per-card success/failure. The previous `adminDeleteModelCardsV2` mutation has been removed.
