Finish an agent-side unwind that failed: a create interrupted while the node's privileged network helper was unreachable left the session's membership published, and the manager then refused to give its VNI and subnet back for as long as the agent ran.
