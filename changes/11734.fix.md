Detect Valkey Sentinel failover via the connected node's ROLE instead of comparing the Sentinel-reported master address, so a demoted master is recognized immediately by the monitor loop and reconnection is triggered through a single round-trip health check.
