Fix the legacy GraphQL `keypairs` query: `keypairs(email:)` failed with `badly formed hexadecimal UUID string`, and `keypairs` without an email returned `user_id: null` for every row, after the `keypairs.user_id` column was dropped.
