Fix session creation failing on label-less images that ship no `ldd` (such as distroless bases) by detecting the C library from the library itself, stop leaking the probe container when the command cannot start, and report host-side Docker failures during the probe as themselves instead of as an unsupported image.
