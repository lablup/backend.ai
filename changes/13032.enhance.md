Gate the app config fragment scoped search with scope RBAC by modelling it as a scope action acting at the single scope the request names, replacing the multi-scope bulk action that reached the processor with no permission validation.
