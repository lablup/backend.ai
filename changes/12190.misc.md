Seed the built-in runtime variant presets (vLLM, SGLang) via an Alembic migration so freshly migrated databases get them without running fixture populate, and sync the install fixture that was missing the SGLang presets.
