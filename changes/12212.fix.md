Rename GraphQL types `DeploymentRevisionPresetValueEntry` and `DeploymentRevisionPresetValueEntryInput` to `RuntimeVariantPresetValueEntry` and `RuntimeVariantPresetValueEntryInput` to reflect that their content is a runtime variant preset value mapping
