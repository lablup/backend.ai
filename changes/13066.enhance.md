Rebuild the scheduler read path on normalized snapshot loading: user-scoped global quota validation, typed slot value families without ResourceSlot, kernel-agnostic agent selection, and a resource-only compute-schedule fitting check.
