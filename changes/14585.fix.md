Start Docker kernel containers on images that ship bash but no /bin/sh by launching the entrypoint through the kernel runner Python, which picks the first POSIX shell the image provides; the kernel runner's user bootstrap and shell commands use the same shell.
