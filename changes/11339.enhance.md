Replace the Redis + event-based AppProxy routing push with a single bulk HTTP endpoint and a lifecycle-hint driven sync handler so Manager → AppProxy traffic flows through one contract and one call per proxy target.
