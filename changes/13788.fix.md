Correct `POST /resource/check-presets` so an agent-less (or slot-less) scaling group reports zero remaining capacity instead of leaking the policy remaining (often `Infinity`) through the clamp, and expose `domain_limits` in the response so clients can display a total consistent with the domain-inclusive `keypair_remaining`.
