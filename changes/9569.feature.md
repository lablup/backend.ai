Deprioritize agents that previously failed for a session during scheduler retry, using Valkey-based per-session tracking with TTL-based auto-cleanup. If all compatible agents have failed, the scheduler falls back to using all of them to avoid blocking.
