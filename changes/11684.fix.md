Convert remaining `BraceStyleAdapter` f-string log calls across accelerator, agent, common health-checker, manager (GraphQL/Harbor/sokovan launcher) and storage artifacts/vfs-storage modules to lazy brace-style formatting to prevent secondary `.format()` failures on values containing literal brace characters.
