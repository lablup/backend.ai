Unify legacy and v2 deployment creation through `DeploymentController`, removing the `current_revision`-direct-assignment bug that bypassed the DEPLOYING strategy lifecycle on initial deploy and dropping the now-redundant `CHECK_PENDING` lifecycle stage.
