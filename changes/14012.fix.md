Retry the initial Valkey connection with exponential backoff so a component that starts before Valkey is ready waits for it instead of exiting, and make the packaged systemd units restart on a clean exit.
