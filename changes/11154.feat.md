Expand deployment and revision search APIs with commonly needed filters and order keys: `domain_name`, `project_id`, `resource_group`, `created_user_id`, `session_owner_id`, `created_at`, `destroyed_at`, `image_id`, `model_id`, `cluster_mode`, `runtime_variant`, plus order options `domain` / `project` / `resource_group` / `tag` (deployments) and `resource_group` / `cluster_mode` / `runtime_variant` (revisions). Also fix `DeploymentOrders.updated_at` crash by replacing the non-existent column reference with `destroyed_at`.
