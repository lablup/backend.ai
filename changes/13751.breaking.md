Rename the domain `modified_at` timestamp to `updated_at`: the column now comes from the shared lifecycle timestamp mixins, and the v2 REST and GraphQL domain APIs report, filter and order on `updated_at`. The legacy GraphQL `Domain` and `DomainNode` fields keep the `modified_at` name.
