Add a scoped search taking several scopes at once to users, projects, domains, resource groups, model cards and agent slot rows, and answer the domain, project and user scope conditions from the ownership graph.
