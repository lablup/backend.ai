Give a registered service an identity derived from its endpoint. A component's worker processes share one listening socket, so an identity per process registered that one endpoint once per worker, and a restart left the old entries behind until the sweep cleared them.
