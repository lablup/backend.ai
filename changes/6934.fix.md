Support service definition override in legacy model service creation and dry run. Service definition override was not supported at all in dry runs, and create only supported limited overrides for custom fields. However, it now supports the same level of service definition as deployments.
