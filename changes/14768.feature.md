Make the runtime variant bulk delete answer per id: the response now lists the deleted ids and, for each id that could not be deleted, its reason, instead of failing the call on the first refused or missing id.
