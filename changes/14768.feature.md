Make the runtime variant bulk delete answer per id: the purged variants and the ids that could not be deleted are reported side by side instead of the call failing on the first refused or missing id.
