Add `extra_headers` support to `BackendAIAnonymousClient`, allowing per-request header injection (e.g. `X-Forwarded-For`, `X-Forwarded-Host`, `X-Forwarded-Proto`) when proxying requests to the manager.
