Seed missing `start_command` into `runtime_variants.default_model_definition` for vllm / huggingface-tgi / sglang / modular-max so legacy deployment_revision rows with `NULL` model_definition can be repaired by `./bai admin deployment revision refresh`
