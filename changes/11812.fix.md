Backfill model_definition on legacy deployment_revision rows that were migrated as NULL, so the post-26.4 agent can launch sessions for them; non-custom variants are repaired automatically, custom variants require `./bai admin deployment revision refresh`
