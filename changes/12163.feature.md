Decouple model-service traffic removal from session teardown: terminating replicas are first drained from AppProxy and their sessions are cleaned up only after the revision's termination grace period (default 30s), letting in-flight requests finish.
