Webserver-issued JWTs now carry the authenticated user's UUID, and the manager authenticates such tokens by the user, deriving the keypair from the user (default keypair first) instead of the access-key claim.
