Record `triggered_by` (the caller) and `acted_as` (the effective/acting user) separately in action reporter messages, so the reporter and the audit-log monitor agree during super-admin impersonation (BEP-1058)
