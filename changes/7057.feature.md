Add `artifact_storages` common table for storage metadata management across object storage and VFS storage backends, and add `updateArtifactStorage` GraphQL mutation for updating artifact storage metadata (e.g., name)