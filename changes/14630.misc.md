Let regular users export their own sessions and keypairs, bulk delete the auto-scaling rules and access tokens of their deployments, and deploy their own model folders, which a superadmin-only gate had refused
