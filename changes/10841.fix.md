Fix non-custom model service creation to accept `model-definition.yml` (in addition to `.yaml`) and to honor a user-supplied `model_definition_path`. The model definition file remains optional for non-custom runtimes — service creation no longer fails when it is absent.
