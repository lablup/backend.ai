Treat `model-definition.yaml` and `model-definition.yml` as interchangeable when validating a model service. Service creation no longer fails when only `model-definition.yml` exists in the model storage but `model-definition.yaml` is requested (or vice versa). The model definition file also remains optional for non-custom runtime variants.
