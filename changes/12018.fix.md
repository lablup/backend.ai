Publish App Proxy Traefik routing config as a single atomic etcd transaction so route updates no longer leave a brief window where a circuit's router points at a deleted backing service, which intermittently dropped in-flight requests during route churn.
