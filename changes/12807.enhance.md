Move the session-enqueue resource-group accessibility check out of the scheduler repository into the scheduling controller so the repository performs DB reads only and no longer requires a project ID for authorization.
