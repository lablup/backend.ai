Backfill NULL `model_definition` in deployment revisions created by pre-26.4.2 migrations by triggering model definition resolution on `modify_endpoint` when the current revision lacks a stored definition.
