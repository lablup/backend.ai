Bind each role to exactly one scope, recorded on the role itself, so that leaving a project takes back only the roles that project holds, and require that single scope when creating a role, which no longer accepts a source.
