Drop the unusable "clear this field" state from eight resource policy, domain, and user update fields whose storage cannot hold a null, and with it the `Sentinel` member the REST schema had been publishing as a default of `1`.
