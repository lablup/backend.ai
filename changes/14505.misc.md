Report a missing user, keypair, project, network, endpoint, auto-scaling rule, vfolder, agent, domain, image, resource group, permission or resource policy under that row's own error code instead of the generic `backendai_read_not-found`.
