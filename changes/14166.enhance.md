Stop returning aiohttp `HTTPException` objects from handlers: 4xx/5xx now raise `BackendAIError` subclasses so their responses carry an `error_code`, and redirects and the logout 200 return a plain `web.Response` instead.
