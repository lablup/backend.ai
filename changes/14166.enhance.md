Stop returning aiohttp `HTTPException` objects from handlers: 4xx/5xx are raised as `BackendAIError` subclasses so their responses carry an `error_code`, and redirects and the logout 200 become plain responses.
