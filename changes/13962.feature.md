Add stored secret key re-encryption: a chunked pass that encrypts every `keypairs.secret_key` again through the configured write provider, on the REST v2 API, a GraphQL mutation, SDK v2, the `bai` v2 CLI and the `mgr secret` admin CLI, which also generates a key encryption key.
