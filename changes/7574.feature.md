Add ARTIFACT entity type to RBAC system and migrate existing artifact data to RBAC database. All artifacts are associated to GLOBAL scope, and permissions are granted to permission groups based on role name suffix where member roles receive read-only access while others get full ownership
