Add a nullable `acted_as` column to `audit_logs` and record the trigger identity (`triggered_by`) and the effective/acting identity (`acted_as`) separately, and audit single-entity RBAC permission denials that occur while a super admin is impersonating a target, so audit logs attribute both actors during impersonation (BEP-1058)
