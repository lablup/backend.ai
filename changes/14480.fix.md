Read an overlay-encryption policy spelled as a boolean: `true` and `false` are documented aliases, but a configuration store that keeps values as text made `false` fall through to the unrecognised-value branch and become `required`.
