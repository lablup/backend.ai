Add Strawberry v2 GraphQL extensions (validation, privilege check, unfrozen guard, metrics, exception handling, logging) and register them at schema creation time for the `/admin/gql/strawberry` endpoint.
