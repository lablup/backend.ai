Gate the app config fragment scoped search with scope RBAC by modelling it as one scope action per domain / user scope, replacing the unauthorized multi-scope bulk action and its `/scoped-search` route with `/domains/search` and `/users/search`.
