start_port = 10205
end_port = 10600

print("entryPoints:")
for port in range(start_port, end_port + 1):
    print(f"  portproxy_{port}:")
    print(f"    address: 0.0.0.0:{port}")
    # print(f"    forwardedHeaders:")
    # print(f"      insecure: true")

remaining_yaml = """
  traefik:
    address: 127.0.0.1:9090

api:
  insecure: true

providers:
  etcd:
    rootKey: "traefik/worker_worker-1-interactive"
    endpoints:
    - 127.0.0.1:8120
  # file:
  #   filename: "{{ bai_home_dir }}/appproxy/traefik/tls.yml"

experimental:
  localPlugins:
    appproxy-traefik-plugin:
      moduleName: backend.ai/appproxy-traefik-plugin
      settings:
        mounts:
        - "{{ bai_home_dir }}/appproxy/traefik:/appproxy-traefik"
    appproxy-traefik-plugin-go:
      moduleName: backend.ai/appproxy-traefik-plugin-go
"""
print(remaining_yaml)
