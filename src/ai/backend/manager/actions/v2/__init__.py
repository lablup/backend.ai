"""The action framework domains are migrating to.

One package per target shape. Replaces the ``BaseAction``-era line under
``actions/{action,processor,validator,monitors}``, which stays only until every
domain is moved over.
"""
