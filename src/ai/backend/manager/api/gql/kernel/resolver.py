"""GraphQL resolvers for kernel queries."""

from __future__ import annotations

# Kernel resolvers can be added here as needed.
# Currently, kernels are accessed through the AgentV2GQL.kernels field.
