---
name: resource-group-adapter-scenarios
type: reference
description: what the resource group adapter guarantees, as scenarios; a group is a global row that is its own scope, so a role sitting on the group reaches it, the name lookup is public so an unknown name is not found for anyone, the allow lists link a group to domains and projects, the fair share spec and the default options are edited apart from the group
scope: src/ai/backend/manager/api/adapters/resource_group
keywords: [resource group, scenario, adapter, rbac, superadmin, allowed, fair share, default options, scheduler]
generated:
  by: claude-code/opus-5
  at: 2026-09-17
status: draft
---
# resource_group 어댑터 — 시나리오

규칙은 상위 디렉터리의 `AGENTS.md`에 있다. 여기 적힌 내용과 실행 결과가 어긋나면 문장 쪽을 먼저
의심한다.

리소스 그룹은 어느 스코프에도 속하지 않게 만들어지고, 그 자신이 에이전트와 세션의 스코프다.
생성과 전체 검색은 호출한 사용자가 슈퍼관리자인지 검사한다(검색은 모니터도 통과한다). 그룹
하나를 지목하는 조회·수정·완전 삭제는 그 그룹에 부여된 권한을 검사하는데, 그룹이 자기
스코프이므로 그룹에 앉힌 역할이 그 권한을 준다. 그래서 이 문서의 권한 짝은 슈퍼관리자와 그
밖의 사용자가 아니라, 그룹에 앉힌 역할을 받은 사용자와 받지 않은 사용자다.

이름으로 id를 찾는 호출은 인증만 확인한다. 그래서 존재하지 않는 이름은 누구에게나 대상을 찾을
수 없어 거부되고, 권한 검사는 찾은 뒤에야 온다.

세션이 리소스 그룹에 닿는 길은 셋이다. 도메인에 건 것, 프로젝트에 건 것, 사용자의 키페어에 건
것. 허용 목록 호출들은 그중 도메인과 프로젝트에 거는 관계를 쓰고 읽는다.

## 생성

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 이름만 주고 만든다 | 슈퍼관리자 | 생성 | `is_active`와 `is_public` 값은 true, `is_default` 값은 false, `description` 값은 비어 있고 `fifo` 스케줄러를 쓰는 노드 전체 |
| `is_default` 값을 true로 지정해 만든다 | 기본 그룹 없음, 슈퍼관리자 | `is_default` 값을 true로 지정해 생성 | `is_default` 값이 true인 노드 |
| 이미 있는 이름으로 만든다 | 그 이름의 그룹 있음, 슈퍼관리자 | 생성 | 이름 중복으로 거부 |
| 기본 그룹이 있는데 또 기본으로 만든다 | `is_default` 값이 true인 그룹 있음, 슈퍼관리자 | `is_default` 값을 true로 지정해 생성 | 기본 그룹이 이미 있다는 이유로 거부 (실행에서 제외, 아래 참고) |
| 슈퍼관리자가 아닌 사용자가 만든다 | 슈퍼관리자 아님 | 생성 | 역할 부족으로 거부 |
| 모니터가 만든다 | 모니터 | 생성 | 역할 부족으로 거부 |

요청의 `domain_name` 값은 읽지 않는다. 그룹은 어느 도메인에도 걸리지 않은 채 만들어지고, 도메인에
거는 것은 허용 목록 절의 호출이다.

기본 그룹은 하나뿐이다. 둘째 기본 그룹을 만드는 시나리오는 현재 코드에서 실패하므로 실행에서
제외했다 (BA-7946). 생성 경로는 그것을 지키는 제약의 위반을 이름 중복과 구분하지 않아 둘째 기본
그룹을 이름 중복으로 거부한다. 수정 경로는 구분한다(수정 절). 시나리오에는 수정 경로와 같은 이유로
거부되는 결과를 기대값으로 적어 두었다. 고쳐지면 다시 실행에 포함한다.

## 조회

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 이름으로 조회한다 | 그룹 하나, 슈퍼관리자 | 이름으로 조회 | 그 그룹 전체 |
| 그룹에 앉힌 읽기 역할을 받은 사용자가 조회한다 | 그룹 하나, 그 그룹 범위의 읽기 역할 보유 | 이름으로 조회 | 그 그룹 전체 |
| 아무 권한도 없는 사용자가 조회한다 | 같은 그룹, 아무 권한도 없음 | 이름으로 조회 | 권한 부족으로 거부 |
| 존재하지 않는 이름으로 조회한다 | 다른 그룹만 있음, 슈퍼관리자 | 이름으로 조회 | 대상을 찾을 수 없어 거부 |
| 아무 권한도 없는 사용자가 없는 이름으로 조회한다 | 다른 그룹만 있음, 아무 권한도 없음 | 이름으로 조회 | 대상을 찾을 수 없어 거부 |
| 있는 것과 없는 것을 id로 섞어 조회한다 | 그룹 둘, 슈퍼관리자 | 세 id를 한 번에 | 요청한 순서대로 반환되고, 없는 id에 해당하는 항목은 비어 있다 |
| 아무 권한도 없는 사용자가 id 여럿을 한 번에 조회한다 | 그룹 둘, 아무 권한도 없음 | 두 id를 한 번에 | 항목마다 권한 부족 거부가 담긴다 |
| 빈 id 목록을 준다 | 그룹 하나 | 빈 id 목록 | 빈 응답. 하위 계층을 호출하지 않는다 |
| 있는 것과 없는 것을 이름으로 섞어 조회한다 | 그룹 둘, 슈퍼관리자 | 세 이름을 한 번에 | 요청한 순서대로 반환되고, 없는 이름에 해당하는 항목은 비어 있다 |
| 아무 권한도 없는 사용자가 이름 여럿을 한 번에 조회한다 | 그룹 둘, 아무 권한도 없음 | 두 이름을 한 번에 | 항목마다 권한 부족 거부가 담긴다 |
| 빈 이름 목록을 준다 | 그룹 하나 | 빈 이름 목록 | 빈 응답. 하위 계층을 호출하지 않는다 |
| 슈퍼관리자가 리소스 현황을 조회한다 | 에이전트가 없는 그룹 하나, 슈퍼관리자 | 리소스 현황 조회 | 용량·사용량·여유가 모두 비어 있다 |
| 읽기 역할을 받은 사용자가 리소스 현황을 조회한다 | 같은 그룹, 그 그룹 범위의 읽기 역할 보유 | 리소스 현황 조회 | 용량·사용량·여유가 모두 비어 있다 |
| 아무 권한도 없는 사용자가 리소스 현황을 조회한다 | 같은 그룹, 아무 권한도 없음 | 리소스 현황 조회 | 권한 부족으로 거부 |
| 없는 그룹의 리소스 현황을 조회한다 | 다른 그룹만 있음, 슈퍼관리자 | 리소스 현황 조회 | 대상을 찾을 수 없어 거부 |

존재하지 않는 이름은 슈퍼관리자에게도 그 밖의 사용자에게도 같은 이유로 거부된다. 이름으로 id를
찾는 호출이 인증만 확인하고, 권한 검사는 찾은 id에 대해서만 실행되기 때문이다. 아래 절들의 없는
이름 시나리오도 모두 같다.

리소스 현황은 `ALIVE` 상태이고 스케줄 가능한 에이전트의 `agent_resource` 행을 더한 것이다.
에이전트가 없는 그룹은 세 값 모두 항목이 없다.

## 검색

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 검색한다 | 그룹 둘, 슈퍼관리자 | 전체 조회 | 둘 다 반환된다 |
| 이름 필터로 검색한다 | 그룹 둘, 슈퍼관리자 | 한쪽 이름과 같은 이름 필터 조회 | 그 그룹 하나 |
| `is_active` 필터로 검색한다 | `is_active` 값이 true인 그룹과 false인 그룹, 슈퍼관리자 | `is_active` 값을 true로 지정한 필터 조회 | `is_active` 값이 true인 그룹만 |
| 첫 페이지만 요청한다 | 그룹 둘, 슈퍼관리자 | `first`만 1로 지정 | 한 건이 반환되고 다음 페이지가 있다고 응답한다 |
| 모니터가 검색한다 | 그룹 둘, 모니터 | 전체 조회 | 슈퍼관리자와 같은 응답 |
| 슈퍼관리자가 아닌 사용자가 검색한다 | 슈퍼관리자 아님 | 전체 조회 | 역할 부족으로 거부 |
| 도메인 스코프에서 읽기 역할을 받은 사용자가 도메인 스코프로 검색한다 | 도메인에 건 그룹 하나와 걸지 않은 그룹 하나, 그 도메인 범위의 리소스 그룹 읽기 역할 보유 | 도메인 스코프로 조회 | 도메인에 건 그룹 하나 |
| 슈퍼관리자가 도메인 스코프로 검색한다 | 같은 그룹 둘, 슈퍼관리자 | 도메인 스코프로 조회 | 도메인에 건 그룹 하나 |
| 아무 권한도 없는 사용자가 도메인 스코프로 검색한다 | 같은 그룹 둘, 아무 권한도 없음 | 도메인 스코프로 조회 | 권한 부족으로 거부 |

스코프 검색은 지목한 스코프마다 그 스코프 안에서 리소스 그룹을 읽을 권한이 있어야 한다. 도메인
스코프의 답은 그 도메인에 건 그룹뿐이다. 슈퍼관리자도 걸지 않은 그룹은 보지 못한다.

## 수정

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 `description` 값만 바꾼다 | 그룹 하나, 슈퍼관리자 | `description` 값 수정 | `description` 값은 새 값, 나머지는 그대로 |
| `is_active` 값을 false로 바꾼다 | 그룹 하나, 슈퍼관리자 | `is_active` 값 수정 | `is_active` 값이 false인 노드 |
| 기본 그룹으로 만든다 | 기본 그룹 없음, 슈퍼관리자 | `is_default` 값을 true로 수정 | `is_default` 값이 true인 노드 |
| 기본 그룹이 따로 있는데 기본으로 만든다 | `is_default` 값이 true인 다른 그룹 있음, 슈퍼관리자 | `is_default` 값을 true로 수정 | 기본 그룹이 이미 있다는 이유로 거부 |
| 값을 하나도 지정하지 않는다 | 그룹 하나, 슈퍼관리자 | 아무 필드도 지정하지 않은 수정 | 아무것도 바뀌지 않은 노드 |
| 그룹에 앉힌 수정 역할을 받은 사용자가 바꾼다 | 그룹 하나, 그 그룹 범위의 수정 역할 보유 | `description` 값 수정 | `description` 값이 새 값인 노드 |
| 아무 권한도 없는 사용자가 바꾼다 | 같은 그룹, 아무 권한도 없음 | `description` 값 수정 | 권한 부족으로 거부 |
| 존재하지 않는 이름을 바꾼다 | 다른 그룹만 있음, 슈퍼관리자 | `description` 값 수정 | 대상을 찾을 수 없어 거부 |
| 설정을 바꾼다 | 그룹 하나, 슈퍼관리자 | `scheduler_type` 값을 `lifo`로, `is_public` 값을 false로, `use_host_network` 값을 true로, `app_proxy_addr` 값을 지정해 설정 수정 | 그 값들이 새 값인 노드 |
| 선점 설정을 바꾼다 | 그룹 하나, 슈퍼관리자 | `preemption` 값을 지정해 설정 수정 | `scheduler.preemption`이 새 값인 노드 |
| 아무 권한도 없는 사용자가 설정을 바꾼다 | 같은 그룹, 아무 권한도 없음 | 설정 수정 | 권한 부족으로 거부 |

수정과 설정 수정은 같은 권한 검사를 거치고 같은 노드를 반환한다. 다른 것은 바꿀 수 있는 필드의
범위다. 수정은 `description`·`is_active`·`is_default`를, 설정 수정은 거기에 `is_public`·
`app_proxy_addr`·`appproxy_api_token`·`use_host_network`·`scheduler_type`·`preemption`을 더한다.
수정 요청 타입의 `name`·`total_resource_slots`·`allowed_vfolder_hosts`·`integration_name`·
`resource_policy` 값은 어댑터가 읽지 않는다.

## 완전 삭제

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 완전 삭제한다 | 그룹 하나, 슈퍼관리자 | 완전 삭제 | 지운 그룹 전체 |
| 그룹에 앉힌 완전 삭제 역할을 받은 사용자가 지운다 | 그룹 하나, 그 그룹 범위의 완전 삭제 역할 보유 | 완전 삭제 | 지운 그룹 전체 |
| 아무 권한도 없는 사용자가 지운다 | 같은 그룹, 아무 권한도 없음 | 완전 삭제 | 권한 부족으로 거부 |
| 존재하지 않는 이름을 지운다 | 다른 그룹만 있음, 슈퍼관리자 | 완전 삭제 | 대상을 찾을 수 없어 거부 |

완전 삭제만 있고 soft delete는 없다. `is_active` 값을 false로 바꾸는 것이 그 자리를 대신한다.

## 허용 목록

도메인 쪽과 프로젝트 쪽, 그리고 그룹 쪽에서 같은 관계를 쓰고 읽는 호출 여덟이다.

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 도메인에 그룹을 건다 | 도메인 하나와 그룹 하나, 슈퍼관리자 | 도메인의 허용 목록에 그룹 추가 | 그 그룹 이름 하나가 담긴 목록 |
| 도메인에서 그룹을 뗀다 | 도메인에 건 그룹 하나, 슈퍼관리자 | 도메인의 허용 목록에서 그룹 제거 | 빈 목록 |
| 없는 그룹 이름을 도메인에 건다 | 도메인 하나, 슈퍼관리자 | 어느 행에도 없는 이름 추가 | 대상을 찾을 수 없어 거부 |
| 없는 그룹 이름을 도메인에서 뗀다 | 도메인 하나, 슈퍼관리자 | 어느 행에도 없는 이름 제거 | 빈 목록. 없는 이름은 건너뛴다 |
| 슈퍼관리자가 아닌 사용자가 도메인에 그룹을 건다 | 도메인 하나와 그룹 하나, 슈퍼관리자 아님 | 도메인의 허용 목록에 그룹 추가 | 역할 부족으로 거부 |
| 슈퍼관리자가 도메인의 허용 목록을 읽는다 | 도메인에 건 그룹 하나와 걸지 않은 그룹 하나, 슈퍼관리자 | 도메인의 허용 목록 조회 | 건 그룹 이름 하나 |
| 도메인 스코프에서 읽기 역할을 받은 사용자가 도메인의 허용 목록을 읽는다 | 같은 그룹 둘, 그 도메인 범위의 리소스 그룹 읽기 역할 보유 | 도메인의 허용 목록 조회 | 건 그룹 이름 하나 |
| 아무 권한도 없는 사용자가 도메인의 허용 목록을 읽는다 | 같은 그룹 둘, 아무 권한도 없음 | 도메인의 허용 목록 조회 | 권한 부족으로 거부 |
| 없는 도메인의 허용 목록을 읽는다 | 도메인 하나, 슈퍼관리자 | 어느 행에도 없는 도메인 이름으로 조회 | 대상을 찾을 수 없어 거부 |
| 슈퍼관리자가 프로젝트에 그룹을 건다 | 프로젝트 하나와 그룹 하나, 슈퍼관리자 | 프로젝트의 허용 목록에 그룹 추가 | 그 그룹 이름 하나가 담긴 목록 |
| 슈퍼관리자가 아닌 사용자가 프로젝트에 그룹을 건다 | 프로젝트 하나와 그룹 하나, 슈퍼관리자 아님 | 프로젝트의 허용 목록에 그룹 추가 | 역할 부족으로 거부 |
| 슈퍼관리자가 프로젝트의 허용 목록을 읽는다 | 프로젝트에 건 그룹 하나, 슈퍼관리자 | 프로젝트의 허용 목록 조회 | 건 그룹 이름 하나 |
| 아무 권한도 없는 사용자가 프로젝트의 허용 목록을 읽는다 | 같은 그룹, 아무 권한도 없음 | 프로젝트의 허용 목록 조회 | 권한 부족으로 거부 |
| 슈퍼관리자가 그룹에 도메인을 건다 | 도메인 하나와 그룹 하나, 슈퍼관리자 | 그룹의 허용 도메인에 도메인 추가 | 그 도메인 이름 하나가 담긴 목록 |
| 없는 도메인을 그룹에 건다 | 그룹 하나, 슈퍼관리자 | 어느 행에도 없는 도메인 이름 추가 | 대상을 찾을 수 없어 거부 |
| 없는 도메인을 그룹에서 뗀다 | 그룹 하나, 슈퍼관리자 | 어느 행에도 없는 도메인 이름 제거 | 대상을 찾을 수 없어 거부. 건너뛰지 않는다 |
| 아무 권한도 없는 사용자가 그룹에 도메인을 건다 | 도메인 하나와 그룹 하나, 아무 권한도 없음 | 그룹의 허용 도메인에 도메인 추가 | 권한 부족으로 거부 |
| 슈퍼관리자가 그룹의 허용 도메인을 읽는다 | 도메인에 건 그룹 하나, 슈퍼관리자 | 그룹의 허용 도메인 조회 | 그 도메인 이름 하나 |
| 읽기 역할을 받은 사용자가 그룹의 허용 도메인을 읽는다 | 어느 도메인에도 걸지 않은 그룹 하나, 그 그룹 범위의 읽기 역할 보유 | 그룹의 허용 도메인 조회 | 빈 목록 |
| 아무 권한도 없는 사용자가 그룹의 허용 도메인을 읽는다 | 같은 그룹, 아무 권한도 없음 | 그룹의 허용 도메인 조회 | 권한 부족으로 거부 |
| 슈퍼관리자가 그룹에 프로젝트를 건다 | 프로젝트 하나와 그룹 하나, 슈퍼관리자 | 그룹의 허용 프로젝트에 프로젝트 추가 | 그 프로젝트 id 하나가 담긴 목록 |
| 아무 권한도 없는 사용자가 그룹에 프로젝트를 건다 | 프로젝트 하나와 그룹 하나, 아무 권한도 없음 | 그룹의 허용 프로젝트에 프로젝트 추가 | 권한 부족으로 거부 |
| 슈퍼관리자가 그룹의 허용 프로젝트를 읽는다 | 프로젝트에 건 그룹 하나, 슈퍼관리자 | 그룹의 허용 프로젝트 조회 | 그 프로젝트 id 하나 |
| 아무 권한도 없는 사용자가 그룹의 허용 프로젝트를 읽는다 | 같은 그룹, 아무 권한도 없음 | 그룹의 허용 프로젝트 조회 | 권한 부족으로 거부 |

도메인 쪽과 프로젝트 쪽에서 거는 호출은 그룹 이름을 id로 바꾸는 자리가 슈퍼관리자 검사를
거치므로 슈퍼관리자만 통과한다. 그룹 쪽에서 거는 호출은 이름을 인증만으로 바꾸고, 관계를 쓰는
자리가 양쪽 스코프의 권한을 검사한다. 어느 쪽이든 아무 권한도 없는 사용자는 거부되지만 이유가
다르다.

떼는 쪽의 없는 이름은 양쪽이 다르게 다룬다. 도메인 쪽에서 없는 그룹 이름을 빼면 건너뛰지만,
그룹 쪽에서 없는 도메인 이름을 빼면 거부된다. 도메인 이름을 id로 바꾸는 자리가 내는 오류를 그
호출이 잡지 않기 때문이다.

프로젝트의 허용 목록 조회는 호출한 사용자의 도메인·지목한 프로젝트·호출한 사용자 자신, 세
스코프에서 읽는다. 세 스코프 모두에서 리소스 그룹을 읽을 권한이 있어야 통과하므로, 이 문서는
슈퍼관리자가 통과하는 것과 아무 권한도 없는 사용자가 거부되는 것만 고정한다.

## fair share 설정

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 설정을 읽는다 | 에이전트가 없는 그룹 하나, 슈퍼관리자 | fair share 설정 조회 | `half_life_days` 7, `lookback_days` 28, `decay_unit_days` 1, `default_weight` 1, `resource_weights`는 비어 있다 |
| 읽기 역할을 받은 사용자가 설정을 읽는다 | 같은 그룹, 그 그룹 범위의 읽기 역할 보유 | fair share 설정 조회 | 역할 부족으로 거부 |
| 없는 그룹의 설정을 읽는다 | 다른 그룹만 있음, 슈퍼관리자 | fair share 설정 조회 | 대상을 찾을 수 없어 거부 |
| 슈퍼관리자가 `half_life_days` 값을 바꾼다 | 그룹 하나, 슈퍼관리자 | `half_life_days` 값 수정 | 그룹 전체 |
| 수정 역할을 받은 사용자가 바꾼다 | 그룹 하나, 그 그룹 범위의 수정 역할 보유 | `half_life_days` 값 수정 | 그룹 전체 |
| 아무 권한도 없는 사용자가 바꾼다 | 같은 그룹, 아무 권한도 없음 | `half_life_days` 값 수정 | 권한 부족으로 거부 |
| 없는 그룹의 설정을 바꾼다 | 다른 그룹만 있음, 슈퍼관리자 | `half_life_days` 값 수정 | 대상을 찾을 수 없어 거부 |

설정 조회는 그룹을 전체 검색으로 찾으므로 슈퍼관리자 검사를 먼저 거친다. 그룹에 앉힌 읽기
역할로는 그룹 자체는 읽어도 이 설정은 읽지 못한다. 수정은 그룹을 지목하는 권한 검사만 거치므로
수정 역할로 통과한다.

`resource_weights`의 항목을 그 그룹 용량에 있는 리소스로 제한하는 검사는 시나리오로 두지 않는다.
서비스가 판단하고 서비스의 단위 테스트가 검사한다. 수정이 반환하는 노드에는 이 설정이 실리지
않는다.

## 기본 옵션

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 기본 배포 옵션을 갈아끼운다 | 그룹 하나, 슈퍼관리자 | 등록된 처리기 하나의 설정을 담아 갈아끼움 | 준 옵션이 통째로 반환된다 |
| 등록되지 않은 배포 처리기를 담는다 | 그룹 하나, 슈퍼관리자 | 등록되지 않은 처리기 이름을 담아 갈아끼움 | 잘못된 입력으로 거부 |
| 수정 역할을 받은 사용자가 기본 배포 옵션을 갈아끼운다 | 그룹 하나, 그 그룹 범위의 수정 역할 보유 | 등록된 처리기 하나의 설정을 담아 갈아끼움 | 준 옵션이 통째로 반환된다 |
| 아무 권한도 없는 사용자가 기본 배포 옵션을 갈아끼운다 | 같은 그룹, 아무 권한도 없음 | 갈아끼움 | 권한 부족으로 거부 |
| 슈퍼관리자가 기본 세션 옵션을 갈아끼운다 | 그룹 하나, 슈퍼관리자 | `priority` 값과 등록된 처리기 하나의 설정을 담아 갈아끼움 | 준 옵션이 통째로 반환된다 |
| 등록되지 않은 세션 처리기를 담는다 | 그룹 하나, 슈퍼관리자 | 등록되지 않은 처리기 이름을 담아 갈아끼움 | 잘못된 입력으로 거부 |
| 아무 권한도 없는 사용자가 기본 세션 옵션을 갈아끼운다 | 같은 그룹, 아무 권한도 없음 | 갈아끼움 | 권한 부족으로 거부 |

처리기 이름은 배포 코디네이터와 스케줄 코디네이터에 등록된 것에서 읽는다. 시나리오는 두
코디네이터를 등록된 처리기만 답하는 대역으로 바꿔 돌린다.

## 아직 적지 않은 것

없다. 어댑터가 제공하는 `create`, `get`, `batch_load_by_ids`, `batch_load_by_names`,
`get_resource_info`, `search`, `scoped_search`, `update`, `update_config`, `purge`,
`update_allowed_resource_groups_for_domain`, `get_allowed_resource_groups_for_domain`,
`update_allowed_resource_groups_for_project`, `get_allowed_resource_groups_for_project`,
`update_allowed_domains_for_resource_group`, `get_allowed_domains_for_resource_group`,
`update_allowed_projects_for_resource_group`, `get_allowed_projects_for_resource_group`,
`get_fair_share_spec`, `update_fair_share_spec`, `admin_replace_default_deployment_options`,
`admin_replace_default_session_options`가 모두 위에 있다.

`batch_load_fields`는 모든 어댑터가 물려받는 공용 도우미이지 이 엔티티의 호출이 아니다. 레포트가
이 호출도 함께 세지만 적을 시나리오는 없다.
