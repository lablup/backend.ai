---
name: container-registry-adapter-scenarios
type: reference
description: container registry adapter scenario guarantees; superadmin-gated management calls, per-id batch load, missing scoped search, RBAC project relations, legacy is_global image visibility, request-type registry validation
scope: src/ai/backend/manager/api/adapters/container_registry
keywords: [container registry, scenario, adapter, superadmin, scoped_search, operation scope, RBAC, allowed groups, is_global, image visibility]
sources:
  - src/ai/backend/manager/api/adapters/container_registry/adapter.py
  - src/ai/backend/manager/services/container_registry/actions/base.py
  - src/ai/backend/manager/services/container_registry/processors.py
  - src/ai/backend/manager/models/container_registry/creators.py
  - src/ai/backend/manager/models/image/creators.py
  - src/ai/backend/manager/models/image/row.py
  - src/ai/backend/manager/repositories/ops/v2/relation/write.py
  - src/ai/backend/manager/api/adapters/image/adapter.py
  - src/ai/backend/manager/api/gql/image/resolver.py
  - tests/scenario/bai_scenario/manager/container_registry
generated:
  by: codex/gpt-5
  at: 2026-09-14
status: draft
---
# container_registry 어댑터 — 시나리오

이 패키지는 `container_registry` API 요청을 레지스트리 및 RBAC 프로세서 호출로 변환한다. 아래
시나리오는 어댑터가 보장하는 권한 검사, 반환값, 오류를 정리한다.

## 대부분의 호출은 슈퍼관리자만 실행할 수 있다

- 각 레지스트리는 상위 스코프에 소유되지 않는 RBAC 엔티티로 생성된다.
- 허용 프로젝트 변경과 여러 ID 조회를 제외한 생성·검색·수정·삭제 호출은 global action으로
  처리되며, 엔티티별 RBAC 권한이 아니라 슈퍼관리자 여부로 접근을 제어한다.
- 허용 프로젝트 추가와 제거는 RBAC 관계 작업을 사용하므로 레지스트리와 프로젝트 양쪽의 권한을
  검사하며, 엔티티 권한 검사 설정의 영향을 받는다.
- 여러 ID 조회는 ID마다 엔티티 권한을 검사하고 결과를 ID별로 반환한다.

## 프로젝트 관계는 권한 그래프에 기록되지만 검색에는 아직 쓰이지 않는다

- 허용 프로젝트를 추가하면 프로젝트가 레지스트리를 `READ` 권한으로 관할하며, 이 권한은
  레지스트리가 소유한 이미지에도 적용된다.
- 레지스트리 검색은 슈퍼관리자용 `admin_search`만 제공하며, 프로젝트 관계를 조건으로 변환하는
  operation scope와 `scoped_search`는 아직 없다.
- 따라서 새 RBAC 그래프에 관계가 있어도 일반 사용자가 접근 가능한 레지스트리나 이미지를 이
  어댑터의 검색 호출로 열거할 수는 없다.

## `is_global` 이미지 공개 정책은 레거시 조회에 남아 있다

- 모든 레지스트리는 상위 소유 스코프 없이 생성되므로 `is_global` 값은 RBAC 소유 관계를 바꾸지
  않으며, 값을 수정해도 기존 프로젝트 관계를 유지한다.
- 레거시 이미지 권한 빌더는 `is_global`인 레지스트리의 이미지를 프로젝트 관계 없이 포함하고,
  `is_global`이 아닌 레지스트리의 이미지는 허용 관계가 있는 프로젝트에만 포함한다.
- 새 관계 그래프는 명시적으로 연결한 프로젝트에 레지스트리와 그 이미지의 읽기 권한을 부여하지만,
  `is_global` 값만으로 모든 프로젝트에 권한을 부여하지는 않는다.
- 이미지 어댑터에는 이 정책을 적용하는 scoped search가 없으므로, 레거시 빌더를 제거하기 전에
  `is_global`인 레지스트리의 이미지 공개 규칙과 목록·단건 조회에 공통으로 적용할 operation
  scope를 정해야 한다.

## 이미지 scoped search는 레거시 접근 조건을 함께 이전해야 한다

- 프로젝트별 이미지 조건은 `is_global`인 레지스트리의 이미지와, `is_global`이 아니면서 프로젝트와
  관계가 있는 레지스트리의 이미지를 선택하고, 호출자 도메인의 `allowed_docker_registries`도
  적용해야 한다.
- customized 이미지는 기존 조회와 동일하게 `creator_id` 컬럼이 가리키는 사용자에게만 노출하거나,
  새 프로젝트 소유권 정책으로 대체한 뒤 목록 조회와 단건 조회가 같은 결과를 내도록 해야 한다.
- 기존 레지스트리·이미지의 권한 그래프를 backfill하지 않으면 새 scoped search가 이전 데이터에
  대한 접근 권한을 찾을 수 없다.

## 생성 시 기본값과 허용 프로젝트 관계를 함께 처리한다

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 필수 값만 지정해 생성한다 | 레지스트리가 없고 호출자가 슈퍼관리자임 | `url`, 이름, `type` 값을 지정해 생성 | `project`·`username`·`password`·`extra` 값은 비어 있고, `ssl_verify`·`is_global` 값은 true인 레지스트리 |
| 허용 프로젝트를 지정해 생성한다 | 프로젝트가 있고 호출자가 슈퍼관리자임 | 허용 프로젝트를 지정해 생성 | 레지스트리와 프로젝트 사이의 관계도 함께 생성됨 |
| 존재하지 않는 프로젝트를 지정해 생성한다 | 프로젝트가 없고 호출자가 슈퍼관리자임 | 허용 프로젝트를 지정해 생성 | 프로젝트를 찾을 수 없어 거부 |
| 일반 사용자가 생성한다 | 호출자가 슈퍼관리자가 아님 | 생성 | 역할 부족으로 거부 |

- `url` 값이나 이름이 공백으로만 이루어진 요청은 요청 타입에서 거부하므로 어댑터 시나리오에
  포함하지 않는다.
- `ssl_verify` 값과 `is_global` 값을 생략해도 둘 다 true가 된다.
- 허용 프로젝트를 포함한 생성도 슈퍼관리자 전용이며, 새 레지스트리에 대한 권한을 미리 부여할
  필요는 없다.

## 검색은 슈퍼관리자만 실행하며 레지스트리 고유 필드로 필터링한다

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 필터 없이 검색한다 | 레지스트리가 2개 있고 호출자가 슈퍼관리자임 | 전체 검색 | 레지스트리 2개와 총 개수 2 |
| `type` 값으로 필터링한다 | `type` 값이 서로 다른 레지스트리가 있고 호출자가 슈퍼관리자임 | `type` 값이 `docker`인 레지스트리 검색 | `type` 값이 `docker`인 레지스트리만 반환됨 |
| `is_global` 값으로 필터링한다 | `is_global` 값이 서로 다른 레지스트리가 있고 호출자가 슈퍼관리자임 | `is_global`이 아닌 레지스트리 검색 | `is_global`이 아닌 레지스트리만 반환됨 |
| 일반 사용자가 검색한다 | 호출자가 슈퍼관리자가 아님 | 전체 검색 | 역할 부족으로 거부 |

필터 결합(`AND`/`OR`/`NOT`), 정렬, 커서·오프셋 페이지네이션은 모든 엔티티가 공유하는 검색
프레임워크의 동작이므로 이 어댑터의 시나리오에 포함하지 않는다.

## 여러 ID를 요청 순서대로 ID별로 반환한다

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 존재하는 ID와 존재하지 않는 ID를 함께 조회한다 | 레지스트리가 2개 있고 호출자가 슈퍼관리자임 | ID 3개를 한 번에 조회 | 존재하는 레지스트리는 요청 순서대로 반환됨. 존재하지 않는 ID에 해당하는 항목의 값은 아직 정해지지 않았음 |
| 빈 ID 목록을 조회한다 | 레지스트리가 2개 있고 호출자가 슈퍼관리자임 | 빈 ID 목록 조회 | 실제 조회를 실행하지 않고 빈 목록을 반환함 |
| 권한이 없는 사용자가 여러 ID를 조회한다 | 호출자가 어떤 레지스트리에도 권한이 없음 | ID 3개를 한 번에 조회 | 요청 전체를 거부하지 않고 ID마다 권한 부족으로 거부됨. 존재하지 않는 ID도 권한 부족으로 거부됨 |

- 여러 ID 조회는 ID마다 읽기 권한을 검사한다. 권한이 없는 ID는 권한 부족으로 거부하고, 존재하지
  않는 ID는 거부와 구분되는 빈 값으로 반환하도록 설계되어 있다.
- 권한 검사가 존재 여부보다 먼저 이뤄지므로, 권한이 없는 사용자에게는 존재하지 않는 ID도 권한
  부족으로 거부된다. 대상이 있는지 없는지는 드러나지 않는다.
- ID별 권한 검사는 슈퍼관리자를 먼저 통과시키지 않는다. 슈퍼관리자도 권한 그래프로 판정하므로
  노드가 없는 ID는 빈 값이 아니라 권한 부족으로 거부된다. 단건 조회의 권한 검사는 슈퍼관리자를
  먼저 통과시키므로 두 검사의 결과가 다르며, 어느 쪽으로 맞출지는 아직 결정되지 않았다.

## 수정 패치는 요청 타입이 검사한다

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| `url` 값만 수정한다 | 레지스트리가 있고 호출자가 슈퍼관리자임 | `url` 수정 | `url` 값만 바뀌고 나머지 값은 유지됨 |
| 수정할 값을 지정하지 않는다 | 레지스트리가 있고 호출자가 슈퍼관리자임 | 빈 수정 요청 | 값이 바뀌지 않은 레지스트리 |
| 허용 프로젝트를 지정해 수정한다 | 레지스트리와 프로젝트가 있고 호출자가 슈퍼관리자임 | 허용 프로젝트 추가와 수정 | 레지스트리 값은 유지되고 프로젝트 관계가 생성됨 |
| 존재하지 않는 ID를 수정한다 | 다른 레지스트리만 있고 호출자가 슈퍼관리자임 | `url` 수정 | 레지스트리를 찾을 수 없어 거부 |
| 일반 사용자가 수정한다 | 레지스트리가 있고 호출자가 슈퍼관리자가 아님 | `url` 수정 | 역할 부족으로 거부 |

- 주소 형식과 Harbor 프로젝트 규칙은 요청 타입(`UpdateContainerRegistryInput`)이 검사한다.
  어댑터에 도달하기 전에 거부되므로 어댑터 시나리오에 적지 않는다.
- 패치는 그 자체로 판단한다. 주소는 값이 있을 때만, 프로젝트는 같은 패치가 Harbor 종류를
  지정하면서 프로젝트 값도 함께 줄 때만 검사한다.
- 저장된 프로젝트가 없는 레지스트리를 Harbor 종류로 바꾸는 수정은 거부되지 않는다. 수정 후의
  행을 다시 검사하지 않기 때문이다.
- `url` 검증은 호스트가 있는지만 확인하며 스킴은 제한하지 않는다. `http` 또는 `https`로
  시작하지 않는 `url` 값에는 `http://`를 붙인 뒤 파싱하므로 `ftp://reg.example`도 통과한다.

## 허용 프로젝트 변경은 레지스트리와 프로젝트 양쪽 스코프의 권한을 검사한다

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 양쪽 스코프에 권한이 있는 사용자가 프로젝트를 추가한다 | 레지스트리와 프로젝트 양쪽에 권한이 있음 | 허용 프로젝트 추가 | 관계가 생성됨 |
| 이미 허용된 프로젝트를 다시 추가한다 | 관계가 이미 있고 양쪽 스코프에 권한이 있음 | 허용 프로젝트 추가 | 기존 관계를 유지하며 중복을 생성하지 않음 |
| 생성 권한만 있는 사용자가 프로젝트를 추가한다 | 양쪽 스코프에 생성 권한만 있음 | 허용 프로젝트 추가 | 관계가 생성됨 |
| 삭제 권한만 있는 사용자가 프로젝트를 추가한다 | 양쪽 스코프에 삭제 권한만 있음 | 허용 프로젝트 추가 | 권한 부족으로 거부 |
| 허용된 프로젝트를 제거한다 | 관계가 있고 양쪽 스코프에 권한이 있음 | 허용 프로젝트 제거 | 관계가 삭제됨 |
| 삭제 권한만 있는 사용자가 프로젝트를 제거한다 | 관계가 있고 양쪽 스코프에 삭제 권한만 있음 | 허용 프로젝트 제거 | 관계가 삭제됨 |
| 생성 권한만 있는 사용자가 프로젝트를 제거한다 | 관계가 있고 양쪽 스코프에 생성 권한만 있음 | 허용 프로젝트 제거 | 권한 부족으로 거부되고 관계가 유지됨 |
| 존재하지 않는 프로젝트를 추가한다 | 프로젝트가 없고 호출자가 슈퍼관리자임 | 허용 프로젝트 추가 | 프로젝트를 찾을 수 없어 거부 |
| 허용되지 않은 프로젝트만 제거한다 | 프로젝트가 레지스트리에 연결되어 있지 않고 호출자가 슈퍼관리자임 | 허용 프로젝트 제거 | 관계를 찾을 수 없어 거부 |
| 프로젝트 권한이 없는 사용자가 프로젝트를 추가한다 | 레지스트리에만 권한이 있음 | 허용 프로젝트 추가 | 권한 부족으로 거부 |
| 레지스트리 권한이 없는 사용자가 프로젝트를 추가한다 | 프로젝트에만 권한이 있음 | 허용 프로젝트 추가 | 권한 부족으로 거부 |
| 엔티티 권한 검사를 비활성화한 뒤 권한이 없는 사용자가 프로젝트를 추가한다 | 엔티티 권한 검사가 비활성화되어 있고 양쪽 스코프 모두에 권한이 없음 | 허용 프로젝트 추가 | 관계가 생성됨 |

- 관계 작업은 레지스트리와 프로젝트를 한 쌍으로 묶어 권한을 검사하며, 둘 중 하나라도 허용되지 않으면
  요청 전체를 거부한다.
- 일반 사용자는 관계 상태를 확인하기 전에 권한 검사에서 거부된다. 따라서 존재하지 않는 프로젝트를
  추가하는 시나리오와 허용되지 않은 프로젝트를 제거하는 시나리오는 슈퍼관리자로 실행한다.
- 엔티티 권한 검사를 비활성화하면 권한이 없는 사용자도 관계를 변경할 수 있다. 생성 작업은
  슈퍼관리자 여부를 직접 확인하므로 결과가 다르다.
- 이미 존재하는 관계를 다시 추가하는 작업은 멱등하다. 제거 요청에 포함된 관계가 하나도 삭제되지
  않았을 때만 오류가 발생한다.
- 추가는 양쪽 스코프의 생성 권한을, 제거는 양쪽 스코프의 삭제 권한을 요구한다. 다른 종류의 권한으로
  대신할 수 없다.
- 관계 변경 시나리오는 호출 결과뿐 아니라 연관 테이블의 행 수를 확인하여 생성·유지·삭제 여부와
  거부된 요청에 부수효과가 없음을 검증한다.

## 삭제는 레지스트리와 허용 프로젝트 관계를 함께 제거한다

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 레지스트리를 삭제한다 | 레지스트리가 있고 호출자가 슈퍼관리자임 | 삭제 | 삭제한 레지스트리의 ID |
| 허용 프로젝트가 연결된 레지스트리를 삭제한다 | 프로젝트 관계가 있는 레지스트리가 있고 호출자가 슈퍼관리자임 | 삭제 | 레지스트리와 프로젝트 관계가 함께 삭제됨 |
| 존재하지 않는 ID를 삭제한다 | 다른 레지스트리만 있고 호출자가 슈퍼관리자임 | 삭제 | 레지스트리를 찾을 수 없어 거부 |
| 일반 사용자가 삭제한다 | 레지스트리가 있고 호출자가 슈퍼관리자가 아님 | 삭제 | 역할 부족으로 거부 |

이 어댑터는 soft delete와 복원을 지원하지 않는다. 삭제하면 레지스트리 행이 데이터베이스에서
제거된다.
