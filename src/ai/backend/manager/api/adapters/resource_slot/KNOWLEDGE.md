---
name: resource-slot-adapter-scenarios
type: reference
description: what the resource slot adapter guarantees for slot types, as scenarios; the name-keyed surface that resolves a name before every call, the superadmin role on create and update against the entity gate on purge, the five referrers that refuse a purge; the agent resource reads are not yet written
scope: src/ai/backend/manager/api/adapters/resource_slot
keywords: [resource slot type, scenario, adapter, superadmin, lookup, purge, in use, public read]
generated:
  by: claude-code/opus-5
  at: 2026-09-11
status: draft
---
# 자원 슬롯 어댑터 — 시나리오

규칙은 상위 디렉터리의 `AGENTS.md`에 있다. 여기 적힌 내용과 실행 결과가 어긋나면 문장 쪽을 먼저
의심한다.

이 어댑터는 두 가지를 다룬다. 슬롯 종류의 카탈로그와, 에이전트가 보고하고 커널이 할당받은
자원의 조회다. 이 문서는 앞의 것만 적는다. 뒤의 것은 자원 그룹 관심사의 몫이라 "아직 적지
않은 것"에 남긴다.

슬롯 종류는 어느 스코프에도 속하지 않게 만들어진다. 생성과 수정은 전역 역할이 있어야 하고,
삭제만 그 슬롯 종류에 부여된 권한을 검사하는데 그 권한을 받을 방법이 없다. 같은 엔티티의 쓰기
셋 중 삭제만 다른 검사를 거치므로, 권한 검사 스위치도 삭제에만 영향을 준다. 조회와 검색은
인증만 확인한다.

인터페이스가 전부 이름 기반이다. id를 받는 호출이 하나도 없고, 조회·수정·삭제는 먼저 이름을
id로 풀어낸 뒤 본 호출을 실행한다. 그 단계에는 권한 검사가 없어, 존재하지 않는 이름은 권한이
없는 사용자에게도 "대상 없음"으로 드러난다.

## 생성

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 이름과 종류만 지정해 생성한다 | 슬롯 종류 없음, 전역 역할 있음 | 이름과 종류로 생성 | 표시용 문자열은 모두 비고, 필수 여부는 거짓, 사용 여부는 참, 순위는 0, 숫자 서식은 십진에 반올림 없음인 노드 |
| 표시 항목을 모두 지정해 생성한다 | 슬롯 종류 없음, 전역 역할 있음 | 표시 이름·설명·단위·아이콘·서식·순위까지 지정해 생성 | 지정한 값이 그대로 담긴 노드 |
| 네 종류 각각으로 생성한다 | 슬롯 종류 없음, 전역 역할 있음 | 개수·바이트·고유·통합 종류로 각각 생성 | 생성된다 |
| 이미 사용 중인 이름으로 생성한다 | 그 이름의 슬롯 종류가 있음, 전역 역할 있음 | 생성 | 이름 중복으로 거부 |
| 슈퍼관리자가 아닌 사용자가 생성한다 | 전역 역할 없음 | 생성 | 역할 부족으로 거부 |
| 권한 검사를 꺼도 슈퍼관리자가 아니면 생성할 수 없다 | 권한 검사 비활성화, 전역 역할 없음 | 생성 | 역할 부족으로 거부 |

이미 사용 중인 이름을 덮어쓰지 않고 거부하는 것은 이 행이 다섯 테이블의 참조 대상이기
때문이다. 조용히 바꾸면 그 이름으로 기록된 할당량이 모두 다른 뜻이 된다.

노드의 id는 uuid가 아니라 슬롯 이름이다. 다른 엔티티에서 id를 무시하는 검사를 여기 그대로
가져오면 이름 검사를 잃으므로, 이 엔티티에서는 id를 검사하고 uuid를 무시한다.

## 조회

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 아무 권한도 없는 사용자가 이름으로 조회한다 | 슬롯 종류 하나, 아무 권한도 없음 | 이름으로 조회 | 그 슬롯 종류 전체 |
| 존재하지 않는 이름으로 조회한다 | 다른 슬롯 종류만 있음 | 이름으로 조회 | 대상을 찾을 수 없어 거부 |

조회에는 권한 검사가 없다. 이름을 풀어내는 단계도 그 뒤의 조회도 인증만 확인하므로, 아무 권한도
없는 사용자가 성공하는 시나리오가 그것을 증명한다. 짝은 생성 절의 역할 부족으로 거부되는
시나리오다.

인증되지 않은 호출은 시나리오로 두지 않는다. 시나리오는 언제나 미리 만들어 둔 사용자로 호출한다.

## 검색

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 아무 권한도 없는 사용자가 검색한다 | 슬롯 종류 둘, 아무 권한도 없음 | 전체 조회 | 둘 다 집계된다 |
| 이름 필터로 검색한다 | 이름이 다른 슬롯 종류 여럿 | 이름 필터 조회 | 그 이름의 슬롯 종류만 남는다 |
| 페이지 크기를 생략한다 | 슬롯 종류 11개 | 크기 없이 조회 | 10건까지 반환되고 다음 페이지가 있다고 응답한다 |

요청 타입이 커서와 종류·표시 이름 필터를 받지만 이 검색은 크기와 건너뛸 수, 이름 필터만 읽는다.
나머지를 함께 주면 지금은 조용히 무시된다. 시나리오를 적기 전에 무시할 것인지 사용할 것인지
정한다.

## 수정

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 표시 이름만 바꾼다 | 슬롯 종류 하나, 전역 역할 있음 | 이름으로 지정해 표시 이름 수정 | 표시 이름은 새 값, 나머지는 그대로 |
| 사용 여부를 끈다 | 사용 중인 슬롯 종류, 전역 역할 있음 | 사용 여부 수정 | 사용하지 않는 상태가 담긴 노드 |
| 값을 하나도 지정하지 않는다 | 슬롯 종류 하나, 전역 역할 있음 | 이름만 지정하는 빈 수정 | 아무것도 바뀌지 않은 노드 |
| 존재하지 않는 이름을 수정한다 | 다른 슬롯 종류만 있음, 전역 역할 있음 | 표시 이름 수정 | 대상을 찾을 수 없어 거부 |
| 슈퍼관리자가 아닌 사용자가 수정한다 | 같은 슬롯 종류, 전역 역할 없음 | 표시 이름 수정 | 역할 부족으로 거부 |
| 슈퍼관리자가 아닌 사용자가 없는 이름을 수정한다 | 다른 슬롯 종류만 있음, 전역 역할 없음 | 표시 이름 수정 | 대상을 찾을 수 없어 거부 |
| 권한 검사를 꺼도 슈퍼관리자가 아니면 수정할 수 없다 | 권한 검사 비활성화, 전역 역할 없음 | 표시 이름 수정 | 역할 부족으로 거부 |

이름과 종류는 수정할 수 없다. 요청 타입이 그 둘을 받지 않으므로 시나리오로 두지 않는다.

여섯 번째 시나리오가 이 절에서 눈여겨볼 곳이다. 이름을 풀어내는 단계가 권한을 검사하지 않으므로,
역할이 없는 사용자도 존재하지 않는 이름에 대해서는 "대상 없음"을 받는다. 역할 부족으로 거부되는
것은 이름이 풀린 다음이다. 삭제 절의 같은 시나리오와 나란히 둔다.

## 삭제

| 시나리오 | 상황 | 요청 | 결과 |
|---|---|---|---|
| 슈퍼관리자가 삭제한다 | 아무것도 참조하지 않는 슬롯 종류, 전역 역할 있음 | 이름으로 삭제 | 삭제한 이름을 담은 응답 |
| 에이전트가 아직 보고하는 슬롯을 삭제한다 | 그 슬롯을 보고하는 에이전트 자원 행이 있음, 전역 역할 있음 | 삭제 | 아직 사용 중이라는 이유로 거부 |
| 커널에 아직 할당된 슬롯을 삭제한다 | 그 슬롯의 할당 행이 있음, 전역 역할 있음 | 삭제 | 아직 사용 중이라는 이유로 거부 |
| 모델 카드가 아직 요구하는 슬롯을 삭제한다 | 그 슬롯을 요구하는 모델 카드가 있음, 전역 역할 있음 | 삭제 | 아직 사용 중이라는 이유로 거부 |
| 배포 preset이 아직 사용하는 슬롯을 삭제한다 | 그 슬롯을 사용하는 배포 preset이 있음, 전역 역할 있음 | 삭제 | 아직 사용 중이라는 이유로 거부 |
| 배포 리비전이 아직 사용하는 슬롯을 삭제한다 | 그 슬롯을 사용하는 배포 리비전이 있음, 전역 역할 있음 | 삭제 | 아직 사용 중이라는 이유로 거부 |
| 존재하지 않는 이름을 삭제한다 | 다른 슬롯 종류만 있음, 전역 역할 있음 | 삭제 | 대상을 찾을 수 없어 거부 |
| 아무 권한도 없는 사용자가 삭제한다 | 같은 슬롯 종류, 아무 권한도 없음 | 삭제 | 권한 부족으로 거부 |
| 아무 권한도 없는 사용자가 없는 이름을 삭제한다 | 다른 슬롯 종류만 있음, 아무 권한도 없음 | 삭제 | 대상을 찾을 수 없어 거부 |
| 권한 검사를 끄면 권한 없이도 삭제된다 | 권한 검사 비활성화, 아무 권한도 없음 | 삭제 | 삭제한 이름을 담은 응답 |

참조하는 곳마다 시나리오를 하나씩 둔다. 다섯 검사가 각각 따로 실행되고, 어느 하나가 빠지면 그
경우는 거부가 아니라 제약 위반으로 실패한다. 한 시나리오로 뭉치면 넷이 빠져도 통과한다.

수정과 삭제가 같은 슬롯 종류에 대해 서로 다른 이유로 거부한다. 수정은 역할 부족, 삭제는 권한
부족이다. 마지막 시나리오가 그 차이의 결과다 — 권한 검사를 끄면 아무 권한 없는 사용자가 슬롯
종류를 삭제할 수는 있지만 수정할 수는 없다. 그것이 의도인지는 이 시나리오를 실행해 본 뒤
정한다.

이 어댑터에는 soft delete가 없다.

## 아직 적지 않은 것

어느 시나리오도 호출하지 않는 어댑터 호출이다. 실행 결과가 이 목록을 함께 출력한다.

에이전트가 보고한 자원과 커널의 할당을 조회하는 일곱 호출은 자원 그룹 관심사에서 적는다. 슬롯
종류의 시나리오는 이 어댑터의 의존성 셋 중 슬롯 종류 처리기만 사용하고, 에이전트와 도메인
처리기에는 접근하지 않는다.

- 에이전트 자원: `search_agent_resources`, `scoped_search_agent_resources`, `get_agent_resource`
- 할당: `search_allocations`, `get_kernel_allocation`
- 개요: `get_domain_resource_overview`, `get_project_resource_overview`

`batch_load_fields`는 모든 어댑터가 물려받는 공용 도우미이지 이 엔티티의 호출이 아니다. 레포트가
함께 세지만 적을 것이 없다.
