"""Backward-compatibility shim for the stream module.

All streaming handler logic has been migrated to:

* ``api.rest.stream.handler`` — StreamHandler class + lifecycle helpers
* ``api.rest.stream`` — route registration
"""
