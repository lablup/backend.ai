"""Tracker orderings applied after the filters.

Each order narrows the candidates to its most preferred tier; the
selection strategy picks among what remains. Orders never exclude — an
all-equal tier keeps every candidate.
"""
